name: TEMP Apply GB Production Witness PR

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

concurrency:
  group: temp-apply-gb-production-witness-pr
  cancel-in-progress: false

jobs:
  apply:
    if: github.event.pull_request.head.ref == 'reconcile/gb-production-witness-2026-07-22'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: reconcile/gb-production-witness-2026-07-22
          fetch-depth: 0

      - name: Apply exact post-merge SSOT reconciliation
        run: python3 scripts/_temp_finalize_gb_nagornaya_bar.py

      - name: Publish scoped permanent files
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          git add \
            projects/gb-is-my-strength/NEXT_AGENT_PROMPT.md \
            projects/gb-is-my-strength/verified/MASTER_BUG_MATRIX.md \
            projects/gb-is-my-strength/reverify/CURRENT_HEAD_REVERIFY_2026-07-22_9c3dec16_nagornaya-bar.md
          test "$(git diff --cached --name-only | wc -l)" -eq 3
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'reconcile(gb): record Nagornaya bar P0 merge [audit-witness]'
          git push origin HEAD:reconcile/gb-production-witness-2026-07-22
