name: Temporary TTS Download Consent Reconciler

on:
  pull_request:
    paths:
      - ".github/workflows/_tmp-close-tts-download-consent.yml"
      - "scripts/_tmp_close_tts_download_consent.py"

permissions:
  contents: write

concurrency:
  group: tmp-close-tts-consent-${{ github.ref }}
  cancel-in-progress: true

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact branch
        uses: actions/checkout@v4
        with:
          ref: agent/close-tts-download-consent-96b7a20f
          fetch-depth: 0

      - name: Detect reconciliation payload
        id: payload
        shell: bash
        run: |
          if test -f scripts/_tmp_close_tts_download_consent.py; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "TTS consent reconciliation is already materialized."
          fi

      - name: Set up Python 3.12
        if: steps.payload.outputs.present == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Apply guarded SSOT reconciliation
        if: steps.payload.outputs.present == 'true'
        run: python3 scripts/_tmp_close_tts_download_consent.py

      - name: Validate AuditRepo structure and rules
        if: steps.payload.outputs.present == 'true'
        run: |
          python3 scripts/check_auditrepo_structure.py
          python3 scripts/validate_audit_repo.py

      - name: Validate matrix coverage and contexts
        if: steps.payload.outputs.present == 'true'
        run: |
          mkdir -p reports/matrix-coverage
          python3 scripts/check_matrix_coverage.py \
            --warn-only \
            --verbose \
            --json-out reports/matrix-coverage/report.json \
            | tee reports/matrix-coverage/report.log
          python3 scripts/matrix_coverage_contexts.py \
            --json-out reports/matrix-coverage/contexts.json \
            --markdown-out reports/matrix-coverage/contexts.md
          python3 - <<'PY'
          import json
          from pathlib import Path
          report = json.loads(Path('reports/matrix-coverage/report.json').read_text(encoding='utf-8'))
          problems = report.get('problems') or report.get('coverage_problems') or []
          if problems:
              raise SystemExit(f"matrix coverage problems: {problems}")
          print('Matrix coverage report contains zero problems.')
          PY

      - name: Verify exact permanent scope
        if: steps.payload.outputs.present == 'true'
        run: |
          test ! -e scripts/_tmp_close_tts_download_consent.py
          test -f projects/gb-is-my-strength/reverify/CURRENT_HEAD_REVERIFY_2026-07-24_96b7a20f_tts-download-consent.md
          git diff --check
          git status --short

      - name: Commit permanent reconciliation
        if: steps.payload.outputs.present == 'true'
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "chore(audit): close TTS download consent P1"
          git push origin HEAD:agent/close-tts-download-consent-96b7a20f
