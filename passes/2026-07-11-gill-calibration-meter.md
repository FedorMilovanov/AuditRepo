# Проход: метр калибровки теста Гилла (CBM) + мёржи

**Дата:** 2026-07-11 (ночной марафон, «боремся и мёржим всё»)
**Репо:** `FedorMilovanov/gb-is-my-strength`
**Ветка:** `lane/system-gill-quiz-calibration-v1` (от `origin/main`, PR #83)
**Эталон:** `references/gill-mobile/gill-mobile-bars-v2.9.html` (`.hlp-*`, RC-03)

## Мёржи (по команде владельца «мёржим всё»)
- **PR #81** (Герменевтика reader-settings + 3 фикса мобилы) → main `4db623f`. CI зелёный.
- **PR #82** (Гилл mobile polish, 8 коммитов) → main `4506c3d`. CI зелёный.
- ⚠️ Локальный `main` оказался «unrelated histories» (старый локальный реф без
  gill-series). Новые ветки надо резать от `origin/main`, не от локального:
  `git checkout -B <lane> origin/main`.

## Фича: метр калибровки (CBM / Certainty-Based Marking) — PR #83
Ядро v2.9-«изюминки» портировано в `GillLearningSheet` → вкладку «Тест»:
- **Гейт уверенности:** варианты `disabled`; сначала выбор уверенности
  (`CONF=[Точно знаю d3, Кажется d2, Наугад d1]`) → `pickConf()` разблокирует.
- **Метр** `#glsQuizCalib`: `calScore/seen*100`%, тон `is-hi`≥70 (зелёный
  #3a9d5d) / `is-mid`≥40 (жёлтый #d4a017) / `is-lo`<40 (красный #c2603a).
- **Вердикт** `#glsQuizVerdict` 3 тона (по смыслу): good (совпадение) / warn
  (уверенная ошибка) / neutral (везение). `calScore++` только за good.
- Сохранены стрик, «Почему это важно», ссылка «Перечитать».
- Файлы: `GillLearningSheet.astro` (разметка+JS), `css/floating-cluster.css`
  (`.quiz-calib/.quiz-conf/.quiz-verdict`, скоуп `[data-gill-v16]`, токены
  `--gb-*` + `html.dark`-оверрайды для good/warn).

### Проверка (скрины @412px, реальные тапы)
- Гейт: `optsDisabledBeforeConf:true` во всех темах; после выбора — разблок.
- Прогон 6 ответов: метр 0→50→33→50→60→67%, классы `is-lo→is-mid`;
  вердикты `is-neutral` (наугад+верно), `is-good` (честно), `is-warn`
  (точно+неверно) — все рендерятся. День/сепия/ночь, 0 JS-ошибок.

## RC-04 (тач-зона скорости 44px) — ✅ СДЕЛАНО ПРАВИЛЬНО (PR #84)
Сначала `::before` резался overflow рейла (короб 32px) → мёртвый CSS, откатывал.
Правильно: `.mobile-speedrail{min-height:44px}` даёт `::before` пилюли
(`inset:-6px`) реальное место внутри короба. 44px-рейл влезает в 58px верхний
бар (`--gill-mobile-topbar-h:58px`) → **прыжка нет**. Проверено: бар 58px и
закрытой, и открытой рельсе (`jump:false`); пилюля визуально 32px; эффективная
тач-высота **42px** (была 32); тап ±3-5px по вертикали попадает в пилюлю.
Ветка `lane/system-gill-speedrail-touch`. НЕ мёртвый костыль — реальная зона.

## Итог
Оба ридера (Гилл+Герменевтика) вычищены и **смёржены в main** (PR #81, #82).
Крупная отложенная фича — метр калибровки — построена, проверена, в PR #83
(на мёрж после CI). Мелкий RC-04 честно отложен (не мёртвый костыль).
