# NEXT AGENT PROMPT — gb-is-my-strength

## Exact authority

- AuditRepo base incorporated before this transaction: `925712d270a7b6babc09ced59a824c5ad28e0111`.
- Product current source anchor: `3d907194d81eee1227a4fc9ad6f037773d19a1ec` (reference-transfer governance + Favorite Store image-origin repair, PR #1076).
- Product exact tested head: `c4cb9c3ce08d7b331c5c6275e91e6e11c944c7ba`.
- Product source rollback anchor before that transaction: `8a51db9a2df74fa615a3eaca698144302e47e332`.
- Production authority remains unchanged: `38b257030afb7cfa8a7b1128f8c86539fd36dec0`, run `30960174778` attempt `1`.
- Candidate: `38b257030afb7cfa8a7b1128f8c86539fd36dec0:30960174778-1`; digest `sha256:973369f7753f89b9a4fae4d19f523f89aa2a50808a0d11cbe8448e79b793c9ef`.
- Current canonical reverify: `reverify/CURRENT_HEAD_REVERIFY_2026-08-06_3d907194_reference-transfer-governance.md`.

## Canonical matrix

- **376 total = 231 closed + 145 open**.
- Open severity counts: P0 `0`, P1 `69`, P2 `26`, P3 `40`, refactoring `7`, AuditRepo `3`.
- `D-22` is closed-current at Product `3d907194`: favorite links remain same-origin-safe and stored image URLs now require `url.origin === location.origin`; exact head passed source **65/65** and Chromium **138/138**.
- `REFERENCE-TRANSFER-GOVERNANCE` is closed-current as one bounded system measure.
- `HOME-P3-FOOTER-EDGE-CONSOLE` is open and intentionally separate from the reference-governance lane.
- New measured unload directions: `R-005`, `R-006`, `R-007`.

## Reference-transfer operating contract

Product now owns:

- `docs/REFERENCE_TRANSFER_POLICY.md`;
- `data/reference-transfer-contracts.json`;
- `scripts/reference-transfer-contracts.mjs`;
- integration through the existing `owner:ui-guard`, with no new duplicate workflow.

Transfer modes are explicit:

1. `exact-replica`;
2. `adaptive-approved`;
3. `native-contract`;
4. `legacy-preserve`;
5. `performance-target`;
6. `inventory`.

Never call a transfer “1:1” merely because text, colors or several class names match. `exact-replica` requires a committed immutable snapshot, verified SHA-256 digest, named target owners, bounded structure/order checks and desktop/mobile visual review.

The guard must remain proportionate:

- no more than **8** blocking transfer contracts;
- no more than **3** delegated guards per contract;
- no more than **12** required and **12** forbidden markers per checked file;
- no automatic harvesting of all HTML tokens/classes/selectors;
- new contracts start advisory;
- `performance-target` and `inventory` never block Product PRs;
- reuse existing route/browser/visual guards instead of adding a mega-suite.

Current registry: **10 contracts = 6 blocking + 4 non-blocking**.

Blocking current ownership protects Favorite Store, Home native premium identity, Gill adaptive mobile chrome, global Search palette, Nagornaya independent world and Karty native data/engine/runtime. Home hero review, Baptists 3D split, TTS route-lazy loading and recursive duplicate inventory remain non-blocking.

## Exact-head evidence for Product PR #1076

- Shared Files Guard `31060246565`: success.
- Favorite Store Contract `31060246524`: source **65/65**, Chromium **138/138**; artifact `8951868661`, digest `sha256:ab2c22aed02bef0da1d622102634d421ab60af0d9aa2d1d71ab8ca3da3036949`.
- Visual Parity Guard `31060246536`: success.
- Metadata & IndexNow Readiness `31060246514`: success.
- Deploy Candidate Contract `31060246497`: success.
- Glossary Contract `31060246525`: success.
- Runtime workflow `31060246529`: dedicated Home Chromium/WebKit job passed production-like build, Pagefind, interaction, real-headed lifecycle and A13 WebKit accessibility.
- Broad interactive audit passed 43 pages / 10 series / 6 quizzes / 3 glossary / 1 footnote / 6 theme / 4 search / 2 media scenarios.

No production or live-deployment claim follows from this source transaction.

## Current bounded lanes

### 1. `HOME-P3-FOOTER-EDGE-CONSOLE`

The separate Home Design Audit reproduced twice on exact head:

`[home-footer-contract] footer touches a viewport edge`

The reference-governance diff did not touch footer/layout/CSS and the dedicated Home Chromium/WebKit job was green. Open a bounded Home visual lane to inspect exact geometry and screenshots. Do not claim visible clipping before evidence, weaken the contract, or mix the repair into unrelated SYSTEM work.

### 2. `R-005` — Baptists 3D measured split

- measure current HTML/JS/CSS before mutation;
- preserve all behavior and strict-native-app ownership;
- target `_app/index.html < 50 KiB` raw with external hashed JS/CSS only if measurements prove the split safe;
- no premature global bundle gate.

### 3. `R-006` — route-scoped TTS loading

- prove which long-form routes own governed TTS;
- keep catalogs and unrelated landings from paying TTS runtime cost;
- record before/after bundle and browser evidence;
- do not introduce a global size assertion before the implementation exists.

### 4. `R-007` — recursive strangler inventory and retirement

- inventory legacy `index.html` and native route ownership recursively;
- classify true duplicates vs intentional independent/legacy-preserve surfaces;
- remove only bounded proven duplicates with route evidence;
- inventory counts never block unrelated Product work.

### 5. Search and rights/provenance

- `SEARCH-P2-07`: proceed only with an authoritative/licensed corpus and explicit rights/provenance evidence.
- Search P3 polish rows may proceed after functional/source owners.

## Before any Product mutation

1. Re-read current Product `main`; do not assume `3d907194` remains HEAD.
2. Read Product `AGENTS.md` and only the surface-specific referenced rules.
3. Inspect current open PR/branch ownership and avoid overlapping active lanes.
4. Identify the current source owner; AuditRepo reports are evidence, not Product authority.
5. Use FAST/LANE/SYSTEM proportionately.
6. For reference work, choose exactly one transfer mode and declare allowed deviations before implementation.
7. Run only checks that can fail from the diff; final required witnesses must cover the exact PR head.
8. Do not move matrix status without current applicable reverify and correct arithmetic.
9. Do not claim production, publication, rights or live behavior without the corresponding exact evidence.

Complete and clean one bounded lane before starting another.
