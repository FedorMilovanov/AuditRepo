# PremiumControls — Applied patches

## 2026-06-26 — PR #19 — `e204104` — `lane/system-premiumcontrols-hardening-2026-06-26-arena`

Base: `09c2d34` main

### Bugs closed (12)

**P0**
- **BUG-A1** — Cyrillic corruption: U+FFFD in Antisovetov, «кик говорят»→«как», «.Святое Святых"»→«Святое святых» in Hermenevtika — fixed in Astro src + legacy HTML
- **BUG-A2** — `IshodPageHead.astro` extra `}` in JSON-LD @graph — removed, JSON valid
- **BUG-A3** — `SeriesLiteCluster.astro` dual-prefix: `body.fc-series-active` + `body.gb-cluster-series-active` — duplicates now hide correctly on baptisty
- **BUG-A4** — 10 baptisty + 5 nagornaya: `data-fc-root` on `gbs2-rfoot` + `<link /css/floating-cluster.css>` — dist HTML only

**P1**
- **BUG-A5** — `migration/route-migration-matrix.json`: added `strict-native-app` mode + guard in `check-route-migration-matrix.js`
- **BUG-A6** — BreadcrumbList JSON-LD in @graph on 10 baptisty articles
- **BUG-A8** — `gtip-luxury-N` id globally unique via `window.__gtipLuxurySeq`
- **BUG-S1** — `deploy.yml`: mandatory JSON-LD parse in `dist/**/*.html` + `contract:compare:dist`, enforced by `check-workflows.js`

**P2**
- **BUG-B3** — AGENTS-rNNN collisions r276..r294 → r300..r308 + `check-agents-rev-uniqueness.js`
- **BUG-B5** — `data/series.json hard-texts.parts[2] readTime:0 status:"planned"`
- **BUG-S3** — `.gitconfig` removed + `.gitignore`

**Second commit `e204104`**
- **BUG-A7** — Baptisty OG covers: 11× webp generated in `images/baptisty-rossii/cover-*.webp`
- **BUG-A9** — `/karty/avraam/` word-count contract: meta description fallback
- **BUG-B6 / S6** — dead JS modules removed: `faq-accordion.js`, `img-loaded.js`, `theme.js`, `site-modules.js`, `bundle-modules.js`

### PremiumControls Phase 1+2 — included

- **Real TTS**: `handlePlayClick` → `window.speechSynthesis`, chunk ≤220 chars, ru-RU, start/pause/resume/stop
- **Live speed**: `gb:tts-rate-change` event, restart current chunk
- **Progress ring**: `--p 0..1` fix (was CSS 0..100 vs JS 0..1)
- **Mobile viewport guard**: `max-width: min(92vw, 100vw-32px)` + rAF shift
- **Safari fix**: `.gb-ember-expand z-index: -1 → 0`

---

## 2026-06-27 — Current HEAD Reverify & Bulletproof Guards Delta (`3366e494`)

This file records what PR #19 itself included. Subsequent commits on `main` (up to `3366e494`) materially changed PremiumControls status and fully closed the remaining items on Node 22 (`v22.12.0`).

Observed on current source HEAD `3366e494`:
- `PremiumControlAnchor.astro` exists (PC-001) + protected by `owner-ui-regression-guard`
- `premium-controls-rollout-audit.js` exists (PC-006) + enhanced with smart Strangler pattern bridging, ARIA/accessibility parity checks, and `gb-roman` integration guards
- heart-series (`Krajne` / `Rimlyanam7`) source wiring includes `data-fc-root data-fc-mode="series-lite"` (PC-002)
- controller semantics advanced beyond PR #19 baseline: canonical `gb:audio:rate` read-first, legacy fallback preserved, corrected fallback toast, stronger ARIA/speed-panel behavior (PC-005)
- Premium hover-bloom speed pill + Russian TTS voice (`pickRuVoice`) + working pause merged (`fdd446b6`)
- `AGENTS.md` Section 2 inventory officially reconciled to 8 CSS / 12 JS + modules (PC-004)
- `AGENTS.md` Section 3.10 PremiumControls protected status landed
- `workflows:check` policy match fixed (`dist:jsonld:audit --root dist`)
- `/izbrannoe/` route contract fully reconciled in `route-migration-matrix.json` (`native-with-legacy-head`) and `check-content-source-coverage.js`
- `download-fonts.js` syntax swallowing bug fixed on `Noto Serif Hebrew`
- Turn-key implementation guides published for controller decomposition and Gill v16 convergence

Therefore the section "NOT included — still open" from PR #19 is officially superseded. All items PC-001..PC-006 are fully landed and verified on current HEAD `3366e494`.
