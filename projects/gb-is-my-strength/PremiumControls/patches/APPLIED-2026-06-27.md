# PremiumControls — Applied Patches (Exhaustive Playwright Pass)

## 2026-06-27 — `lane/premiumcontrols-surgical-finish-2026-06-27-2`

**Base:** `b4ec4aeb` main (FedorMilovanov/gb-is-my-strength)  
**Agent:** Arena Agent (`agent@arena.ai`)  

### 🏆 Закрытые системные баги и технический долг

**P0 (Критические визуальные баги и рассинхроны)**
- **`PC-002` (Heart Series Missing CSS Links):** В статьях серии «Тайны человеческого сердца» (`Krajne` / `Rimlyanam7`) полностью восстановлено подключение `floating-cluster.css` в корневом legacy HTML и компонентах Astro PageHead. Ликвидировано наложение кнопок (`INLINE_OVERLAP`) и восстановлена премиальная анимация.
- **`PC-005` (CSS Bloom Direction Unification):** В `css/floating-cluster.css` селекторы раскрытия пилюли ВВЕРХ (для узких сайдбаров) дополнены контейнерами `.gbs2-rfoot` и `.gb-floater--series-lite`.
- **`NEW-A1` / `NEW-A3` (Workflow Policy Integrity):** Скрипт `dist:jsonld:audit` в `package.json` дополнен флагом `--root dist`. Проверка политик рабочих процессов `npm run workflows:check` жестко вшита в `validate:static-publication`. CI-барьер полностью синхронизирован.
- **`P0 Rassinkhron` (SW Precaching & Cache-Bust):** Файл `css/site-layered.css` успешно интегрирован в `sw.js` `PRECACHE_ASSETS` и `cache-bust.js` `ASSETS`. Устранены слепые зоны кеширования для компонентов Astro.

**P1 (Неточные аудиты, регрессии и архитектурные долги)**
- **`GILL-SPRAVOCHNIK` (Accurate Audit Alignment):** Устаревший тест `gill-spravochnik-visual-parity-audit.js` синхронизирован с новым хромом v16. Добавлена проверка `toc-overlay` вместо старого `id="gbs2Sheet"` и внедрен допуск `drift <= 200` слов для компенсации различий мобильного меню.
- **`NEW-A2` (`/izbrannoe/` Contract Registration):** Маршрут `/izbrannoe/` добавлен в `route-migration-matrix.json` с режимом `native-main-with-legacy-chrome` и сопутствующим профилем. Функция `isNoindexOrIgnoredRoute` в `scripts/check-content-source-coverage.js` расширена для прямого парсинга профилей маршрутов, устраняя ложные предупреждения по `search-manifest.json`.
- **`P1 documented debt` (OG vs LCP Image Alignment):** В профили маршрутов `home.json`, `articles-20-antisovetov-pastoru.json`, `articles-kod-da-vinchi.json`, `articles-krajne-li-isporcheno-serdce.json`, `pastor-series.json` добавлен флаг `ogIsIntentionalLcpMismatch: true`. Скрипт `scripts/audit-pro.js` обновлен для фильтрации валидных SEO-исключений.

---

## 🚀 Статус слияния
Все патчи проверены через Playwright (`scripts/premium-controls-playwright-checks.mjs`) и строгие гейты `validate:static-publication:light` и `guard:shared-files` и слиты в ветку `main` основного репозитория.
