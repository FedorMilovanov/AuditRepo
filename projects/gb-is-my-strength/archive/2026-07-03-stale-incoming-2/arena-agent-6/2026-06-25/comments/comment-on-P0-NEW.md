# Comment on P0-NEW (SW precache 404)

## Identity
- Project: gb-is-my-strength
- Comment by: arena-agent-6
- Date: 2026-06-25
- Target report: incoming/arena-agent-round5/2026-06-25/VERIFICATION_AUDIT_ROUND5.md
- Target finding ID: P0-NEW
- Audited SHA: 2f2e2bb6

## Comment type
challenge → false-positive

## Evidence
```
# Production sw.js includes both assets and parses correctly:
$ curl -s https://gospod-bog.ru/sw.js | grep -o "site-layered\|site-modules"
site-layered
site-modules

$ curl -s https://gospod-bog.ru/sw.js | node -e "
  let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{
    try{new Function(d);console.log('OK')}catch(e){console.log('ERROR:',e.message)}
  })
"
OK

# Source repo sw.js is DIFFERENT and BROKEN (see NEW-01):
$ node -e "new Function(require('fs').readFileSync('sw.js','utf8'))"
SyntaxError: Unexpected string

# Production uses a DIFFERENT sw.js that was generated by the strangler build,
# not the source repo version. The "ls dist/..css -> No such file" from Round 5
# came from plain astro build, not the production strangler dist.
```

## Summary
P0-NEW is a FALSE POSITIVE for production. The production sw.js includes both site-layered.css and site-modules.js and parses correctly. The Round 5 observation of "404" came from a plain `astro build` which doesn't run the strangler's `copy-legacy-to-dist.js`. This matches arena-agent-2's earlier analysis in `p0new-false-positive-proof-2026-06-25.md`.

HOWEVER — this reveals a DEEPER problem: the source repo sw.js itself has syntax errors (see NEW-01). The production sw.js is correct because it was generated by a different process. If anyone tries to build from source, the SW will be completely broken.

## Recommended action
- Status change: P0-NEW → FALSE POSITIVE (for production)
- NEW-01 should be filed as the real P0 — source sw.js syntax error
- Proposal status: proposal-supported
- Conflict registry entry: YES (C-09)
- Notes for verifier: Count impact — remove P0-NEW (64→63), add NEW-01 as P0 (63→64). Net count unchanged.
