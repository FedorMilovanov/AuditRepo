# Arena Agent intake — 2026-06-25

Здесь лежит стартовый пакет отчётов по проекту `gb-is-my-strength`.

Принцип:
- файлы в этой папке считаются входящими отчётами;
- не переписывать как «истину последней инстанции»;
- верификатор должен перенести подтверждённые выводы в `working/` и `verified/`.
