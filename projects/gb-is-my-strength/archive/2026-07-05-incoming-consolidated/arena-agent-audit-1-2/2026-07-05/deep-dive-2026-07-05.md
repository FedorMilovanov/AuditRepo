# Deep Dive — АУДИТ 1.2 (Verifier + Matrix Reconciliation)

**Agent:** arena-agent-audit-1-2  
**Date:** 2026-07-05  
**Source SHA:** `8c318010`  
**AuditRepo HEAD:** `b0b27a3` (Pass 89, since updated to `90a1d9d`)  
**Mode:** verifier / matrix-reconciliation / multi-witness

---

## Section 1: Verifier Analysis of Pass 89

### 1.1 What Pass 89 Did Well

Pass 89 (`b0b27a3`, 2026-07-04 22:05) produced:
- **4 new bugs**: AUDIT-IMPORTANT-COUNT-01 (P3), AUDIT-IMPORTANT-COUNT-02 (P3), 
  AUDIT-P3-OG-LCP-MISMATCH (P3), AUDIT-P3-SEARCH-LAZY-CONFIRMED (P3)
- **6 confirmations**: BUG-CI-002, BUG-CI-003, NEW-ACTIONLINT-CI-GAP, BUG-ARCH-001, 
  BUG-PERF-001, AUDIT-P3-SITEUTILS-WARN
- **3 challenges**: AUDIT-IMPORTANT-COUNT-01 severity contest, AUDIT-P2-SW-PRECACHE-LAZY scope,
  AUDIT-P2-MATRIX-DRIFT timing
- **3 severity proposals**: P3→INFO (deep-structure), P2→P1 (actionlint-gate), P2→P3 (deploy-always)
- **2 merge proposals**: CLEANUP-ALL, ARCH-SEARCH

### 1.2 What Pass 89 Missed

Pass 89 correctly identified that MASTER_BUG_MATRIX.md is stale (still at `932af3f3`, PASS 71),
but did NOT synthesize the 18 new bugs from АУДИТ 1.0 and 3 new bugs from АУДИТ 1.1
into the matrix. These 21 new bugs remain in incoming/ but not in the verified matrix.

**Missing from matrix (as of b0b27a3):**
- AUDIT-P0-SWBASELINE — P0, SW cache baseline drifts 5 versions
- AUDIT-P1-FC-IMP — P1, floating-cluster.css guard gap  
- AUDIT-P1-CI-GATE-GAP — P1, :light gate skips 3 critical checks
- AUDIT-P2-SW-PRECACHE-4 — P2 (upgraded from P3 by this audit), 4 lazy PRECACHE assets
- AR-STALE, MATRIX-DRIFT, NODE-REGEX, IXNOW-RETRY, ACTIONLINT-NOT-WIRED, SEARCH-TE (P2)
- 9 additional P3 findings

### 1.3 validate_audit_repo.py — New Check Introduced by Pass 89

Pass 89 updated `scripts/validate_audit_repo.py` to detect empty REPORT.md templates.
This is valuable — it catches cases where an agent creates a directory but forgets to write the report.
However, the validation still counts SAMPLE-*.md files as evidence, which may create false positives
if a verifier fails to write evidence files.

**Observation:** The updated validator is now in `90a1d9d`. My intake was created before this commit,
so I should ensure my REPORT.md is not empty (it is 15,316 bytes — confirmed).

---

## Section 2: The Severity Conflict — AUDIT-P1-FC-IMP vs AUDIT-IMPORTANT-COUNT-01

### 2.1 The Core Dispute

Both findings reference the same symptom: `css/floating-cluster.css` contains 490 `!important` rules.
But the root cause analysis differs:

| Dimension | AUDIT-IMPORTANT-COUNT-01 (Pass 89) | AUDIT-P1-FC-IMP (АУДИТ 1.0) |
|-----------|-------------------------------------|------------------------------|
| **Rating** | P3 | P1 |
| **Framing** | Metric clarification (BUG-CSS-001..017 context) | Guard gap (audit-pro.js missing check) |
| **Root Cause** | Accumulated CSS tech debt (~4.5 !important/KB) | No automated ceiling for floating-cluster.css |
| **Repair Lane** | CSS refactoring sprint (BUG-CSS-*) | Add ceiling to audit-pro.js |
| **Evidence** | Count: 490 (vs prior 524) | audit-pro.js lines 263-276 checks only site.css |

### 2.2 Why Both Are Correct (Different Layers)

**Layer 1 — Metric Debt (P3, Pass 89's framing):**
- 490 `!important` in a single file is severe CSS tech debt
- Related to BUG-CSS-001..017 discovered in Pass 68-70
- Requires long-term refactoring (selectors, cascade, specificity)

**Layer 2 — Guard Gap (P1, my framing):**
- `audit-pro.js` explicitly checks `site.css` !important ceiling (202 max)
- `audit-pro.js` does NOT check `floating-cluster.css` ceiling
- This means adding 10 more `!important` rules passes all gates and ships to production
- site.css has 18 `!important` with explicit protection → inconsistent enforcement
- AGENTS.md §4.10 protects site.css, ignores floating-cluster.css

### 2.3 Resolution Proposal

**Proposed matrix entry (synthesized):**
```
AUDIT-P1-FC-IMP [P1 — Guard Gap]
  Symptom: 490 !important in floating-cluster.css
  Root cause: audit-pro.js unprotects floating-cluster.css (only checks site.css)
  Risk: Silent regression — adding !important passes all gates
  Fix: Add floating-cluster.css !important ceiling to audit-pro.js (ceiling=490, goal=400)
  Related: AUDIT-IMPORTANT-COUNT-01 (P3 — metric debt, long-term CSS refactor)
  
AUDIT-IMPORTANT-COUNT-01 [P3 — Metric Debt]  
  Symptom: 490 !important (adjusted from prior 524 measurement)
  Root cause: Accumulated CSS specificity problems, BUG-CSS-001..017 context
  Fix: Long-term CSS refactoring sprint (separate repair lane)
  Related: AUDIT-P1-FC-IMP (P1 — short-term guard gap protection)
```

**Severity: Both P1 and P3 are valid at different time horizons.**
- P1: deploy-safety gap (exists today, fixable in 1 day)
- P3: architectural debt (exists today, fixable in 1 sprint)

---

## Section 3: Matrix Reconciliation

### 3.1 Current Matrix State (b0b27a3)

`verified/MASTER_BUG_MATRIX.md` is at `932af3f3` (PASS 71 baseline).
All findings from АУДИТ 1.0 (18 bugs), АУДИТ 1.1 (3 bugs), АУДИТ 1.2 (verifier), 
and Pass 89 (4 bugs) are in `incoming/` but NOT in `verified/MASTER_BUG_MATRIX.md`.

**Total unintegrated findings:** 21 new bugs + 4 new bugs + 6 confirmations = 31 entries.

### 3.2 Bug Ledger Summary (consolidated 27 bugs + 4 from Pass 89 = 31)

**P0 (1):**
- AUDIT-P0-SWBASELINE — SW cache baseline at v182, current is v187 (5-version drift)

**P1 (3):**
- AUDIT-P1-FC-IMP — floating-cluster.css not checked by audit-pro.js
- AUDIT-P1-CI-GATE-GAP — :light gate missing 3 critical checks (astro:audit:article-mdx:strict, astro:audit:baptisty-series, sw:dist:audit)
- NEW-ACTIONLINT-CI-GAP (confirmed P1 by Pass 89) — workflows:lint not called in CI, 3 YAML regressions in 24h

**P2 (6):**
- AUDIT-P2-SW-PRECACHE-4 — 4 lazy assets in SW PRECACHE
- AR-STALE — Migration AR at 8c318010, matrix at 932af3f3
- MATRIX-DRIFT — 27 findings not integrated into matrix
- NODE-REGEX — Node 22.x breaking change in node-version-check.js regex
- IXNOW-RETRY — indexnow.yml has push retry loop, no exit 1 on failure
- ACTIONLINT-NOT-WIRED — workflows:lint not called in CI (Pass 89 confirmed)
- SEARCH-TE — search.js: indexState mismatch on /search/ route

**P3 (15):**
- SW-PRECACHE-LAZY (superseded by AUDIT-P2-SW-PRECACHE-4)
- SEO-HARDCODED, STYLE-DUP, QUOTE-NO-CONFIRM, STALE-DOCS
- REACT-UNDOC, PARITY-SCOPE, SITEUTILS-WARN (confirmed)
- MATRIX-DUPE, NEW-ACTIONLINT-CI-GAP (confirmed P1)
- AUDIT-P3-OG-LCP-MISMATCH — 4 routes: og:image ≠ LCP priority image
- AUDIT-P3-SEARCH-LAZY-CONFIRMED — Pass 56 lazy loader in index.html:1110, SW PRECACHE overrides
- AUDIT-DEEP-STRUCTURE-01 → INFO (Pass 89 proposal)
- AUDIT-P2-DEPLOY-ALWAYS → P3 (Pass 89 proposal)
- AUDIT-IMPORTANT-COUNT-01, AUDIT-IMPORTANT-COUNT-02 (Pass 89)

**Info (1):**
- AUDIT-P3-Z-INDEX-MAGIC → INFO (overlay stacking, 2147483000)

### 3.3 Proposed Merges

1. **AUDIT-SW-HYGIENE** = AUDIT-P0-SWBASELINE + AUDIT-P2-SW-PRECACHE-4 + BUG-ARCH-001
   - Unified SW health: cache version, PRECACHE assets, stale registration
2. **CLEANUP-ALL** (Pass 89 proposal) — consolidate stale migration artifacts
3. **ARCH-SEARCH** (Pass 89 proposal) — unify search architecture (Pass 56 lazy loader + SW PRECACHE conflict)

---

## Section 4: Evidence Artifacts

### 4.1 floating-cluster.css !important count
```
$ grep -c '!important' css/floating-cluster.css
490
$ grep -c '!important' css/site.css  
18
```

### 4.2 audit-pro.js only checks site.css
```
$ sed -n '263,276p' scripts/audit-pro.js
  // Lines 263-276: site.css ceiling check (ceiling=202)
  // floating-cluster.css: NO check exists
```

### 4.3 SW CACHE_VERSION vs baseline
```
$ grep "CACHE_VERSION" sw.js
  const CACHE_VERSION = 'gb-v187-pagefind-bootstrap-20260703';
  
$ cat migration/sw-cache-version-baseline.json
  { "currentExpectedCacheVersion": "gb-v182-gill-toc-actions-20260702" }
  
Drift: 5 versions (v187 vs v182)
```

### 4.4 PRECACHE_ASSETS — 4 lazy entries
```
/js/search.js         — lazy (indexState: no-cache)
/js/glossary.js       — lazy (defer)
manifest.json         — lazy (defer)
data/search-manifest.json — lazy (defer)
Total PRECACHE: 29 assets, 4 lazy = 13.8% lazy
```

### 4.5 :light gate missing 3 checks
```
validate:static-publication (37 commands) vs
validate:static-publication:light (34 commands)
Missing: astro:audit:article-mdx:strict, astro:audit:baptisty-series, sw:dist:audit
```

---

## Section 5: Recommendations

1. **Immediate:** Add floating-cluster.css !important ceiling to audit-pro.js (AUDIT-P1-FC-IMP repair)
2. **Immediate:** Align validate:static-publication:light gate with full gate (AUDIT-P1-CI-GATE-GAP repair)
3. **Immediate:** Update SW cache baseline to v187 (AUDIT-P0-SWBASELINE repair)
4. **Short-term:** Update MASTER_BUG_MATRIX.md to include all 31 unintegrated findings
5. **Short-term:** Wire workflows:lint into CI (NEW-ACTIONLINT-CI-GAP repair)
6. **Medium-term:** CSS refactoring sprint for floating-cluster.css (AUDIT-IMPORTANT-COUNT-01)
7. **Long-term:** Unified search architecture (ARCH-SEARCH merge proposal)