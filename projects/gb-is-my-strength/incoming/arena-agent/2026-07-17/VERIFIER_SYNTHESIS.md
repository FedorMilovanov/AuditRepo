# Provisional Verification Wave Synthesis

## Meta

- Project: `gb-is-my-strength`
- Verifier role: `arena-agent` synthesizing its own bounded package; independent verification still required
- Local date: `2026-07-17`
- Product anchor: `cb3681e1a85b5f8919c9dc537f812a842bbe9235`
- Wave purpose: classify one publication-gate candidate, deduplicate six symptoms, and define the evidence still required before MASTER promotion
- Scope: `DATA-CONSISTENCY-PUBLIC-ASSET-RESOLUTION` plus directly relevant historical/current reports
- Signal classes: audit-harness, control-plane, lifecycle, live artifact
- Explicit exclusion: no claim of authenticated remote workflow failure

## Inputs reviewed

| Input | Anchor | Contribution |
|---|---|---|
| This intake `REPORT.md` and evidence | `cb3681e` + live | direct failure, source mechanism, live assets, workflow topology |
| `incoming/arena-auditor-2026-07-14/2026-07-14/REPORT.md` | `2ca2af3b` | historical PASS for the same command; introduction-window boundary |
| `incoming/bugverifikator/2026-08-19/VERIFIER_SYNTHESIS_gb-is-my-strength_2026-08-19.md` | `cb3681e` + live | same-anchor Product consolidation; local build/runtime explicitly excluded |
| `reverify/VALIDATION_SYSTEM_AUDIT_2026-07-05.md` | `d1941a6d` | historical validation-topology/root-assumption context |
| Current MASTER and system themes | AuditRepo snapshot | duplicate/active-owner check |
| Product PR census | PRs `#1721`, `#1722` | adjacent ownership/collision boundary |

## Executive result

One compact candidate is supported by four evidence angles. The six error lines are duplicate symptoms. No reader-facing missing image exists. The package is strong enough for an independent current-check wave, but this self-synthesis does not directly mutate MASTER because a real Git checkout/current PR-head check and negative fixture are still absent.

## 1. Current defect candidate

### Keep for independent verification

`DATA-CONSISTENCY-PUBLIC-ASSET-RESOLUTION`

- Current at inspected anchor: yes, deterministic local command.
- Mechanism: root-only physical resolver rejects valid Astro `public/` owner.
- Required-path relevance: yes, publication aggregate and three workflow entry points.
- Proposed class: current-local audit-harness/control-plane defect.
- Proposed severity: P1 provisional; remote release-incident status unproven.

## 2. Duplicate symptoms

The following six reported missing images must not become six findings:

- `07-angels-kept-under-darkness.webp`
- `09-gospel-preached-to-the-dead.webp`
- `08-spirits-in-prison.webp`
- `06-enoch-prophesied-and-apostolic-witness.webp`
- `03-what-is-first-enoch.webp`
- `04-book-of-watchers.webp`

All have one source mechanism and one repair/acceptance boundary.

## 3. Confirmations and compatible history

- The historical `data:consistency` PASS at `2ca2af3b` is not challenged. It constrains the finding to later drift/migration.
- The bugverifikator cb3681e synthesis is not contradicted because it excluded local build/runtime regression and classified Product rows, while this is a harness/control-plane candidate.
- The historical validation-system audit is supported only at theme level; its old inventories are not treated as current authority.

## 4. Rejected or non-current signals

- Git-enumeration failures from a ZIP snapshot: environment.
- Browser launch failure from missing system library: environment.
- BnF DNS failures: environment, contradicted by independent rendered fetch.
- Legacy root schema dimensions: reference-only; production dist passes.
- MDX `4Q204` warning: non-blocking frontmatter regex noise.
- Atlas checkbox label warning: false positive; labels wrap controls.

None should enter MASTER from this wave.

## 5. Root cause

Publication URL ownership and physical source ownership are not resolved through one declared contract. The checker assumes URL `/x` maps only to repository-root `x`, while current Astro publication also maps `public/x` to `/x`.

The repair belongs to owner resolution, not image duplication, route content, or six allowlist entries.

## 6. Severity and owner decisions

No owner decision is presently required for semantics. The remaining decisions are verification/ownership checks:

1. Does current main or exact PR `#1722` already supersede the resolver?
2. Does a real checkout reproduce the same direct failure?
3. Is P1 justified by current required topology alone, or should it remain P2 until a remote blocked run is shown?

Default proposal: P1 operational candidate because required gates are deterministically red; never call it P0/release incident without authenticated run evidence.

## 7. Repair and verification wave

Required next wave:

1. checkout exact `cb3681e` or record a new current anchor;
2. inspect exact heads/files of PRs `#1721` and `#1722`;
3. run direct checker unchanged;
4. confirm one root-owned and one `public/`-owned positive fixture;
5. add/run a genuinely missing negative fixture;
6. run required aggregate and applicable workflow contracts;
7. if green after repair, close/remove the active row in the same wave.

Bounded repair proposal: `proposals/proposal-owner-aware-image-resolver-repair.md`.

## 8. Canonical recommendation and sufficiency

| Decision | Recommendation | Sufficiency |
|---|---|---|
| Preserve raw evidence | Yes | complete |
| Treat six errors as one root | Yes | strong |
| Add six Product bug rows | No | contradicted by live/artifact evidence |
| Promote one MASTER row now | Not from self-synthesis alone | needs independent checkout/current-overlap witness |
| Queue bounded verification | Yes | ready |
| Mutate Product from this AuditRepo PR | No | out of scope |
| Upgrade to P0 release incident | No | remote exact-run witness absent |

Final package state: `needs-independent-verification`, `proposal-open`, one candidate, zero direct MASTER edits.
