# Глубокий аудит качества прорисовки и подложек картографического раздела (/karty/)

**Дата:** 2026-07-20  
**Целевой репозиторий:** `FedorMilovanov/gb-is-my-strength`  
**HEAD commit:** `32ae0d7d62bee81737a9aae1f136946d047fe4fb`  
**Статус:** **FALSIFIED TOP-QUALITY IN BOTH LIVE AND DRAFT PIPELINES / CRUTCHES & WEAK SOLUTIONS VERIFIED**

---

## Executive Summary / Заключение по качеству

Тщательный глубокий анализ алгоритмов рендеринга, исходных файлов векторной географии (`base-geo.svg`, `base-geo-mediterranean.svg`, `base-geo-urheimat.svg`, `avraam/base.svg`), оффлайн-движка листов `scripts/lib/sheet-engine.js` и структур данных `route.json` показал, что **даже черновой визуал («эталонные листы» в `sheet-engine.js`) НЕ является топ-качеством**, а опирается на примитивные геометрические овалы, линейные прямоугольные формулы координат, тайловые узоры и неполные данные.

Принятый за ориентир канон Owner Aesthetic Canon («ВАРИАНТ 1: Эстетика: древняя карта в стиле атласа») требует:
- Атмосферных исторических карт на пергаментном/слоновом фоне с гравированным рельефом местности;
- Точной картографической сетки координат с учетом картографической проекции;
- Чёткого отображения территорий и наделов колен с пастельной цветовой заливкой и границами;
- Живописных волн акватории вдоль берегов;
- Уникальных архитектурных икон мест для ВСЕХ карт;
- Изогнутых исторических торговых путей, огибающих естественные препятствия без заплывания в воду.

В реальности в проекте действуют прямо противоположные «костыли» **как в веб-клиенте, так и в оффлайн-генераторе листов**:

1. **Примитивные овалы вместо горных хребтов в `sheet-engine.js` (`RELIEF-P1-01`):**
   Рельеф гор в «эталонном» `sheet-engine.js` выполнен не гравюрными штрихами или топографическими горизонталями, а **вытянутыми геометрическими SVG-овалами** (`<ellipse cx="678" cy="538" rx="10" ry="62" fill="url(#mountainHatch)"/>`), заполненными линейной штриховкой. Для всего Средиземноморья горный рельеф состоит всего из 3 овалов, а для карты «Начало» рельеф равен пустой строке (`urheimat: ''`).

2. **Непроекционные линейные уравнения координатной сетки (`GRAT-P1-01`):**
   В `sheet-engine.js:437-441` пересчет градусов в координаты выполняет простейшие линейные уравнения `x = 623 + (lon - 35.22) * 100`, расценивающие Землю как плоскую 2D-плоскость без картографической проекции. Сетка `graticule()` рисует только засечки на внешней рамке, но **не проводит меридианы и параллели по полю карты**.

3. **Зацикленный тайл волн 20×20px на морскеой поверхности (`SEA-P1-01`):**
   Паттерн `#seaPattern` повторяет крошечный плиточный квадрат 20×20px с двумя синусоидами волн по всему морю, создавая эффект механической кухонной плитки на огромном холсте 1900×1430 вместо живописных прибрежных волн.

4. **3-линейные завитковые уголков картуша (`ORN-P1-01`):**
   Угловые виньетки рамки `#cornerOrn` состоят из 2 элементарных дуг и точки (`<path d="M15,3 H6 Q3,3 3,6 V15"/>`), сильно уступая настоящей гравюрной каллиграфии старинных атласов XVI–XVIII веков.

5. **Мёртвый массив `halos` и размытие букв обводкой (`HALO-P1-01`):**
   В `sheet-engine.js:579` объявлен пустой массив `halos = []`, который так и не заполняется. Гало подписей имитируется через CSS-свойство `stroke: rgba(245,237,216,.88)` на теге `<text>`, которое при мелком кегле (10–11px) заплывает внутрь просветов букв, мыля шрифт.

6. **Неразветвляющиеся сплайны Катмулла-Рома (`ROUTE-P1-01`):**
   В `sheet-engine.js:531` трассировка маршрутов выполняется единой непрерывной математической кривой `catmullRom(routePts)`. Без ручной расстановки костыльных точек `route_via` сплайн дугой заплывает в моря и озёра.

7. **Полное отсутствие иконок у 82% карт в наборах данных (`GLYPH-P1-01`):**
   В 9 из 11 файлов `route.json` количество иконок ровно 0. В 82% карт даже оффлайн-генератор рендеряе стандартные дефолтные кружки.

---

## Детальный реестр всех 27 выявленных дефектов прорисовки

| Код | Уровень | Компонент / Файл | Суть проблемы и влияние на качество |
| :--- | :--- | :--- | :--- |
| **RELIEF-P1-01**| **P1** | `sheet-engine.js:182` | **Черновик:** Горы отрисованы примитивными SVG-овалами `<ellipse>`; для `urheimat` рельеф отсутствует |
| **ROUTE-P1-01** | **P1** | `sheet-engine.js:531` | **Черновик:** Неразветвляющийся сплайн Катмулла-Рома заплывает в море без костыльных точек `route_via` |
| **GLYPH-P1-01** | **P1** | `route.json` (9/11) | **Черновик:** 9 из 11 наборов данных карт содержат 0 иконок `glyph`, вырождаясь в кружки |
| **GRAT-P1-01**  | **P1** | `sheet-engine.js:437` | **Черновик:** Линейные аффинные уравнения координат искажают геометрию; линии сетки на карте отсутствуют |
| **SEA-P1-01**   | **P1** | `sheet-engine.js:52`  | **Черновик:** 20px плиточный паттерн волн даёт механический эффект «кафельной плитки» на море |
| **ORN-P1-01**   | **P1** | `sheet-engine.js:98`  | **Черновик:** Угловой орнамент `#cornerOrn` — примитивная 3-линейная дуга без гравюрной детализации |
| **HALO-P1-01**  | **P1** | `sheet-engine.js:579` | **Черновик:** Неиспользуемый массив `halos = []`; обводка `<text>` размывает мелкие буквы 10–11px |
| **REG-P1-01**   | **P1** | `map-engine.js` | В браузерном движке отсутствует код `route.regions`: 13 полигонов уделов в `shvatim` не видны |
| **PERF-P1-01**  | **P1** | `avraam/base.svg:28` | Бесконечная 14s SVG-анимация `feTurbulence` вызывает непрерывную переристовку холста (15–20 fps) |
| **UI-P1-01**    | **P1** | `map-engine.js:690` | Абсолютное позиционирование `.me-search` перекрывает кнопку «Назад» на 390px экранах |
| **BASE-P1-01**  | **P1** | Basemap SVGs | Пустой `<defs>` во всех 3 базовых подложках (`base-geo.svg`, `mediterranean.svg`, `urheimat.svg`) |
| **BASE-P1-02**  | **P1** | `map-engine.js:2612` | Принудительное `opacity="0.5"` на `me-base-geo`, обесцвечивающее рельеф местности в 2 раза |
| **BASE-P1-03**  | **P1** | `avraam/base.svg:4` | Тёмный градиент `#22241f` + 6 слоев анимированного звёздного неба, заслоняющие карту |
| **BASE-P1-04**  | **P1** | `base-geo.svg:47` | 18 отсутствующих ID-линковок, включая вершины гор (`#hill`, `#peak`, `#peak-snow`, `#canaanRidge`) |
| **ARCH-P1-01**  | **P1** | Architecture | Раскол движков: пергаментный стиль изолирован в `sheet-engine.js`, а веб-клиент рендерит тёмную схему |
| **SVG-P1-01**   | **P1** | `atlas-export/*.svg` | Файлы экспорта содержат неэкранированные HTML-сущности `&nbsp;`, ломающие XML-парсеры |
| **FONT-P1-01**  | **P1** | `map-engine.js:463` | `Georgia, "Times New Roman"` для иврита в `.hw` вызывает сброс в системный sans-serif |
| **MINI-P1-01**  | **P1** | `map-engine.js:1126` | Миникарта не содержит геометрии морей/суши (точки над чёрным окном) + global monkey-patch `flyTo` |
| **WAYP-P1-01**  | **P1** | `map-engine.js:1321` | Точки археологии рендерятся 7px серым текстом без подложек, сливаясь с фоном рельефа |
| **CSS-P1-01**   | **P1** | `map-engine.js:380` | Вызов `destroy()` удаляет `<style id="me-base-css">`, обесцвечивая остальные карты на странице |
| **SIG-P1-01**   | **P1** | `map-engine.js:1388` | Оверлеи кампаний используют жесткие пиксельные офсеты (`origin.x - 74`), перекашиваясь при зуме |
| **DATA-P0-01**  | **P0** | `map-engine.js:1297` | Прямолинейный костыль `M...L...L...` через горы; авторские кривые `stages[].paths` игнорируются |
| **DRAW-P1-03**  | **P1** | `map-engine.js:1528` | 100% мест рендерятся простыми плоскими кружками `r=4.5`; 0 архитектурных иконок |
| **ENGINE-P1-23**| **P1** | `map-engine.js:1492` | Анимация при наведении анимирует `nth-child(3)` (пунктирный бейдж) вместо маркера `dot` |
| **TEXT-P1-01**  | **P1** | `map-engine.js:1550` | Моноширинный расчёт ширины плашки (`length * 0.6`) обрезает широкие буквы (`Ш`, `Ж`, `ש`) |
| **BASE-P2-01**  | **P2** | Sub-basemaps | Грубая геометрия побережий (всего 123 и 68 команд SVG на целые регионы) |
| **DATA-P2-01**  | **P2** | `karty/*/route.json` | Полное отсутствие описаний кривых путей `stages[].paths` у 10 из 11 карт в репозитории |
