# SD-15 — Vosk / genealogy / NF rows on 2273b8c9

Direct source inspection on ref 2273b8c9.

## NEW-VOSK-FETCH-NO-ABORT — FIXED (source-verified)
- Claim: "280MB model fetch(MODEL_URL) not wired to AbortController; continues after navigation/opt-out."
- vosk-tts-engine.js now has `modelDownloadController` + `if(modelDownloadController && !modelDownloadController.signal.aborted){ try{modelDownloadController.abort()} }` (lines 299-306). Download is cancellable. FIXED candidate.

## NEW-VOSK-DEAD-SPLITSENTENCES — STILL OPEN
- vosk-tts-core.js:413 `function splitSentences(text,maxLen)`; :446 exported `splitSentences`. No call site
  anywhere (grep in vosk.js/vte.js: only definition + export; controller uses splitTtsChunks). Dead export
  persists. STILL OPEN.

## AR-AUDIT-17 — STALE/FIXED (no longer reproducible)
- Claim: "validate:all fails with 2 inline script syntax errors in genealogy-build/atlas-template.html and interactive-template.html".
- validate:all = validate:strict (node scripts/validate.js --strict) && seo-audit.
- validate.js validateJS() (lines 421-435) runs `node --check` ONLY on `../js/*.js`; `scripts/` is in the
  `skip` set of walkHtmlFiles (line 468). Genealogy html templates are NOT syntax-checked and NOT part of
  validate:all.
- The inline "errors" are build-time placeholders `const ATLAS=/*__ATLAS__*/;` / `const GRAPH=/*__GRAPH__*/;`
  replaced at generation (built data/genealogy/v2/build/atlas-interactive.html contains real data, no placeholder).
- => AR-AUDIT-17 is stale/fixed-current candidate (the gate no longer fails on these templates).

## NF-DEAD-ENHANCE-SHIM — needs source recheck (not fully verified here)
- Not re-verified in this pass (floating-cluster-controller.js not inspected). Mark "reverify".

## Net
- FIXED candidates: NEW-VOSK-FETCH-NO-ABORT, AR-AUDIT-17.
- STILL OPEN: NEW-VOSK-DEAD-SPLITSENTENCES.
- REVERIFY: NF-DEAD-ENHANCE-SHIM.
