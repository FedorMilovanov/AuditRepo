# Intake — chatgpt / 2026-08-19

## Identity

- Project: `gb-is-my-strength`
- Source repo: `FedorMilovanov/gb-is-my-strength`
- Agent: `ChatGPT GPT-5.6 Sol`
- Audited anchor: Product `main` `bcb41e57d7f9c011ac597c51a240fba19152a908`
- Exact-current reverify boundary: Product `main` `01894214765d7ab6e51a7eea1fb7f239c6591af8`
- Product movement across those anchors touched CSS-layer admission, not the runtime/search/SW/TTS owners retained below.
- AuditRepo base: `6aae4f35a7f308d364f924bc41ea9796e99dd34f`
- Product mutation: none
- MASTER mutation: none

## Evidence files

| File | Role |
|---|---|
| `REPORT.md` | Initial forensic synthesis for Scripture representation/oracle and historical button-audit completeness. |
| `SCRIPTURE_OCCURRENCE_ANCHOR_FALSE_WITNESS.md` | Current false-fragment manifestation: `data-note-id` accepted as `id`; 13 occurrences / 12 reference labels / 4 false fragment values; producer/verifier common-mode. |
| `SECURITY_NOSNIFF_OWNER_LAYER.md` | Separates CSP HTML/meta ownership from transport `X-Content-Type-Options`; no fabricated live missing-header claim. |
| `SW_SCRIPTURL_ROUTE_VERSION_CHURN.md` | Corrected semantic SW registration census: **70/85** Astro routes register; **0** duplicate owners; **0** bare worker URLs; five current script identities: `1`×25, `1781282355`×22, `1778943682`×19, `20260802`×2, `c7f8b6e9`×2. |
| `SW_CACHE_TRANSACTION_GENERATION_GAP.md` | Failed successor install can delete a cache namespace still owned by the active worker because cache generation is not isolated from worker generation. |
| `SW_REVISIONED_CANONICAL_DOWNGRADE.md` | A revisioned asset miss can downgrade to bare canonical cache bytes from the controlling generation; existing browser guard proves only same-generation fallback. |
| `BROWSER_MATRIX_ZERO_WORKER_FAILOPEN.md` | Malformed worker-count env can collapse all-public browser matrices to zero workers and a vacuous `0/0 PASS`; current workflow literals remain valid. |
| `BUTTON_RUNTIME_GENERATED_SURFACE_WITNESS.md` | Source-surface manifestation A: corrected Astro+TSX button count is 49; runtime JS adds 26 generated missing-type controls, so literal DOM-producing source surface is at least 75 / 25 files. |
| `ASSET_REVISION_SOURCE_SURFACE_FALSE_GREEN.md` | Source-surface manifestation B: deploy `cache-bust.js` excludes JS internal URL constructors; two stale managed revision literals survive while the hard gate reports synchronization. Current canonical TTS ownership keeps these specific stale literals from being promoted to a normal-path Product outage. |
| `ANTISOVETOV_STRATEGIC_MAP_RUNTIME_ORPHAN.md` | Strategic-map manifestation retained under the article migration root. |
| `ARTICLE_ENHANCEMENTS_PARTIAL_MIGRATION_ROOT.md` | System root: strict-native transport removal lacked semantic capability-completeness; strategic map, FAQ, heading anchors and reversible cards lost owners. |
| `ARTICLE_CAPABILITY_CI_ARTIFACT_WITNESS.md` | Exact-head Product CI artifact genuinely exercises Gill chrome/TTS but not retained legacy-capability families. |
| `ARTICLE_CAPABILITY_MIGRATION_CONTROL_PLANE_GAP.md` | Strict-native guards prove legacy removal, not capability parity/cardinality. |
| `ARTICLE_INTERACTIVE_AUDIT_SCOPE_WITNESS.md` | Production-like Playwright audit is real but has a narrower semantic matrix than the missing capability families. |
| `THEME_MULTIWRITER_CURRENT_REVERIFY.md` | Negative reverify: legacy `theme` writes are a coordinated compatibility bridge around canonical ReaderPreferences. |
| `GENEALOGY_ISLAND_FAULT_BOUNDARY_REVERIFY.md` | Narrows genealogy resilience issue to the client-only interactive island; static page content survives outside it. |
| `TTS_SHAREDWORKER_CLIENT_LIFECYCLE.md` | SharedWorker disconnect does not promptly retire a disappeared model-load waiter; last live cancel can fail to abort the shared ~280 MB fetch. |
| `TTS_SHAREDWORKER_SPEECH_DISCONNECT_WITNESS.md` | Same TTS root: abandoned closed-client synthesis can remain in the single global synthesis queue. |
| `SEARCH_LAZY_LOADER_FAILURE_STATE.md` | Lazy-loader manifestation A: `MobileChromePage` leaves `__gbSearchBootRequested=true` after failed `search.js`; `/karty/` and `/konfessii/` then cannot retry before reload. |
| `TTS_ENGINE_SCRIPT_FAILURE_STATE.md` | Lazy-loader manifestation B: canonical Reader TTS can attach listeners to an already-failed preloaded Vosk script after its terminal error event, leaving `engineScriptPromise`/`warmPromise` pending and explicit retry unable to create a fresh acquisition. |
| `CURRENT_MASTER_DISPOSITION_2026-08-20.md` | Verifier reduction pass over the 13 old MASTER rows: keep/narrow/absorb/retire rather than mechanically adding new rows. |
| `CURRENT_MASTER_EXACT_REVERIFY_01894214.md` | Exact-current reverify of retained old manifestations plus negative checks on fragments/ARIA/search-manifest identity. |

## Eight forensic work units

1. **`SCRIPTURE-OCCURRENCE-REPRESENTATION-ORACLE`** — exact-search context and fragment identity are derived through lossy/raw-source representations. Current false fragments: **13 occurrences / 12 labels / 4 values**. Producer/dist/browser oracles share narrower surrogates and can green together.
2. **`SOURCE-SURFACE-AUDIT-FALSE-COMPLETENESS`** — multiple guards overstate corpus completeness because runtime-producing JavaScript is outside scanned source classes. Button evidence: historical exhaustive `47` becomes **49 Astro+TSX** and at least **75** with JS-generated controls. Asset evidence: **536** revisioned local literals independently checked, **534 exact / 2 stale**, while deploy `cache-bust.js` returns success because its corpus excludes JS internal resource URLs. This is an audit-harness/system integrity root, not a claim that every omitted literal is a current user-visible bug.
3. **`SECURITY-NOSNIFF-OWNER-LAYER-MISMATCH`** — source heads express `nosniff` as an HTML pragma, but transport header ownership is a different enforcement layer. No live vulnerability claim without response-header measurement.
4. **`SW-ROOT-GENERATION-AUTHORITY`** — semantic census: **70/85** Astro routes register one root SW under **five** route-dependent script identities (`1`×25, `1781282355`×22, `1778943682`×19, `20260802`×2, `c7f8b6e9`×2), with **0** duplicate registration owners and **0** bare `/sw.js` registering routes. The same root has non-generation-isolated failed-install rollback and cross-generation revision downgrade. Absorb the old narrow `SW-PWA-FRESHNESS` residual here.
5. **`BROWSER-MATRIX-ZERO-WORKER-FAILOPEN`** — malformed worker env can execute zero browser cases yet report success; ordinary CI currently passes safe literals.
6. **`ARTICLE-LEGACY-CAPABILITY-PARTIAL-MIGRATION-ROOT`** — strict-native migration removed broad legacy owners correctly but did not prove retained capability completeness: strategic map (39 triggers / 36 payloads), FAQ (17 buttons), enabled heading-anchor copy controls across at least 8 series-native heads, and 6 reversible cards currently lack their former owner/replacement.
7. **`TTS-SHAREDWORKER-CLIENT-LIFECYCLE`** — no authoritative normal-disconnect owner. A disappeared client can remain a model-load waiter and can also leave its synthesis work in the global queue.
8. **`LAZY-RUNTIME-LOADER-FAILURE-STATE`** — failed resource acquisition is not reliably returned to a retryable state. Search manifestation: MobileChrome sticky `bootRequested` blocks every later request after a script error. TTS manifestation: an already-failed preloaded Vosk `<script>` can be discovered later, listeners are attached after its terminal event, and `engineScriptPromise`/`warmPromise` can remain pending indefinitely; explicit retry then returns the same pending state. Search and TTS require one invariant: `failed -> settled -> retryable`, while still deduplicating a genuinely in-flight request.

## Current reverifies that reduce old MASTER work

- **`AR-IDX-JS-02-MULTIWRITER`** — recommend retire/absorb as a current conflict. Canonical `gb:reader-preferences:v1` owns truth; legacy `theme` is a deliberate compatibility mirror with same-document reconciliation, Sepia-safe cross-tab handling and wired regression coverage.
- **`GENEALOGY-NO-ERROR-BOUNDARY`** — keep but narrow to interactive-island fault containment; do not describe the entire page as blank.
- Security direct CSP rows should be absorbed under one `FRAGMENTED-SECURITY-OWNERSHIP` system root while retaining separate CSP-meta and transport-header enforcement owners.
- Old `SW-PWA-FRESHNESS` should be absorbed under `SW-ROOT-GENERATION-AUTHORITY`.
- Button Product/residual counting should not rely on the disproved `47` exhaustive census.

## Important negative/correction boundaries

- **SW census self-correction:** earlier evidence said `67/85` + `bare /sw.js ×7`. That scanner missed indirect `BaseLayout` registration and mis-associated config ownership. Correct result is **70/85**, **five identities**, **bare ×0**.
- Asset-revision TTS negative: all **57** Astro route graphs with a real floating-controller owner also mount `ReaderActionsRuntime`; canonical TTS loads current Vosk through `assetUrl()` and capture-claims normal play/keyboard paths. The two stale floating literals are guard-corpus evidence, not a routine current TTS fetch claim.
- TTS script-loader finding is different: it concerns the canonical `ReaderActionsRuntime` preloaded Vosk script failing before a later `ensureVoskScript()` call observes it.
- Series `.reveal` remains readable through explicit fallback.
- Search async result generation/cancellation showed no stale-result resurrection; work unit 8 concerns resource-loader failure/retry state.
- PWA manifest identity/icons are internally consistent.
- Current literal same-page fragments: 77 checked, 0 missing targets across 85 Astro route graphs.
- Current literal ARIA relationships: 924 checked, 0 missing literal targets.
- Route projection cardinality matches declarative policy: sitemap 76/76, curated search manifest 75/75, RSS 58/58. Known RSS defect remains date-value authority, not route membership.
- Search manifest: 76 items / 76 unique IDs / 0 duplicate IDs.
- 380 current `_blank` source links all include `noopener`.
- ReaderState route identity/progress reverify: 48 series routes / 0 legacy key collision groups.
- Telemetry route graph: 53 routes with exactly one Metrika init, 32 without, 0 duplicate init; without a declared sitewide analytics requirement this remains owner-decision/observability evidence.
- No current CSP-blocked runtime origin or direct persisted-storage XSS sink found.
- Tooltip trigger↔popup AT relationship remains parked pending accessibility-tree/AT witness.

## Collision / evidence boundary

- Product PR #1752 owns `data/scripture-search-index.json`; this branch does not regenerate or repair it.
- No competing Product repair lane was found for SW, article capability migration, TTS or lazy Search at the recorded boundaries.
- Concurrent AuditRepo MASTER/matrix owners are untouched; all material stays under this unique incoming path.
- Local browser navigation is administratively blocked. Local production-like build is also environment-blocked by incomplete snapshot `node_modules` (`astro` missing); neither environment failure is treated as Product evidence.

## CI boundary

Exact head `22e9066be62c54dcb2b760d4b6b251747feefd25` reached terminal `success` in both `AuditRepo Validate` and `AuditRepo Workflow Preflight`. `TTS_ENGINE_SCRIPT_FAILURE_STATE.md` and this index update changed the branch afterward, so that success is now historical. Only terminal runs on the final exact branch head may be called green.