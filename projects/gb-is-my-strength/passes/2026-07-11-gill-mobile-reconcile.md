# Проход: мобильный ридер Гилла — полировка + реконсиляция к v2.7

**Дата:** 2026-07-11
**Репо:** `FedorMilovanov/gb-is-my-strength`
**Ветка:** `lane/system-gill-mobile-polish-v1` (от `main`, НЕ смёржена)
**Эталон (канон):** `auditrepo/references/gill-mobile/gill-mobile-bars-v2.9.html`
**Статус:** 🔄 в работе

> v2.9-рефайнменты поверх стабильной базы (баров/вкладок/part-TOC/панелей):
> - TOC «Поделиться» → **«Распечатать · PDF»** (`window.print()`) — шэр уже в нижнем баре.
> - RC-01: overlay part-TOC/поиска ПОВЕРХ контента, фикс при прокрутке.
> - RC-02: в ночи пропадал текст выбранной кнопки (конфликт специфичности) — починить.
> - RC-03: вердикт калибровки = 3 честные градации (зелёный=точная, нейтральный=
>   угадал-наугад, жёлтый=только уверенная ошибка/misconception) — цвет = смыслу.
> - RC-04: кнопки скорости → тач-зона 44px.
> База v2.7→2.9 стабильна — реконсилю её сплошным проходом, рефайнменты v2.9 накладываю сверху.

> Обновление v2.8 (канон): (1) баг «оглавление уезжает/просвечивает» — оверлей
> part-TOC (`.btoc-overlay`) и поиск (`.cp-backdrop`) должны лежать ПОВЕРХ области
> контента и стоять намертво при прокрутке, а НЕ внутри прокручиваемого
> контейнера (иначе затемняющий фон скроллится с текстом и не накрывает его).
> Проверить это на живом part-TOC/поиске Гилла. (2) Тесты: процент калибровки —
> отдельный визуальный метр в шапке (полоса + %, зелёный ≥70% / жёлтый 40-69% /
> красный ниже), а не строка текста.

## Контекст

Восстановление после упавшей сессии. Задача владельца: привести **мобильный вид
Гилла к эталонам 1:1** (или лучше), без «вольностей». Ключевой урок этого прохода:
чинить точечно = ходить по кругу. Правильный подход — **сверять весь экран с
каноническим референсом** (`references/gill-mobile/`), а не править «на глаз».

## Что уже сделано (коммиты в ветке)

- `8cf8201` — вкладки листа «Обучение» влезают (было переполнение 69px); таблицы
  не ломают слова (min-width + гор.скролл); скрепки-якоря `.heading-anchor`
  скрыты на мобилке (31→0); кнопки настроек `nowrap`.
- `2eac624` — лист сжимается по контенту (убран пустой низ); крестик × вверху-
  справа (был сиротой снизу-слева); шрифт кнопок `clamp` (не режет «Свободный»).
- `3af2b56` — вкладки листа обучения по v2.6: **Термины-first**, «Поиск» — иконкой
  в конце + разделитель, мягкая заливка выделения; **термины строятся при
  открытии листа** (на init было рано → пусто, `termsBuilt` guard трипался).

## Ключевые находки

- **«Баговання Германевтика» (крошки списком, бар за экраном) — НЕ баг сайта.**
  Это артефакт моей неполной сборки: `astro build` без шага
  `copy-legacy-to-dist.js` не кладёт `css/site.css`, `css/floating-cluster.css` в
  `dist` → глобальный CSS 404 → страница без стилей. **Для скринов только полная
  `npm run strangler:build:production-like`.**
- **part-TOC (оглавление части) — 3 бага в живом коде** (не мои правки): двойные
  рамки, «римские цифры» рендерятся точкой `·`, нет навигации между частями.
  Эталон v2.7 (`.btoc-*`, ~строки 609-660): пункт = только левая акцент-полоска
  (`border-left:2px`, скруглён справа), БЕЗ рамки-коробки; номер — мелкий справа,
  акцентный когда активен; есть прогресс-бар и шэр-футер; поиск = командная
  палитра `.cp-*`.
- **Аудит reader v5** (`references/gill-mobile/gill-reader-v5-AUDIT.md`): «Свободный»
  нужен `clamp` (не голый `nowrap`); дубли цитат в `js/highlights.js` (нет
  дедуп-гарда перед `unshift`); «скрепка у Настройки» = артефакт вебвью, не наш код.

## Карта файлов (где что живёт)

- **Мобильные бары + листы:** `src/components/article-pilots/gill-series/`
  - `GillSeriesMobileBar.astro` — верхний/нижний бар, тема-цикл, inline-скорость.
  - `GillLearningSheet.astro` — лист «Обучение» (вкладки Термины/Конспект/Тест/
    Заметки + поиск-иконка); `buildTerms()` зависит от `SiteUtils.hydrateGlossaryTerms`.
  - `GillReaderSettingsSheet.astro` — лист «Настройки» (тема/размер/интервал/ширина).
  - `GillSeriesChrome.astro` — корневой `[data-gill-v16]` (на `.gbs2-world`).
  - part-TOC / series-TOC — оверлеи здесь же; данные — `gillSeriesData.ts`.
- **Общий CSS:** `css/floating-cluster.css` (легаси, копируется в dist). Мои
  mobile-оверрайды — блок `@media (max-width: 63.99em)` ~строка 3897+.
- **Общий JS/рантайм:** `js/floating-cluster-controller.js` (open/close оверлеев,
  `initTocPopups`), `js/highlights.js` (выделения/цитаты), `js/glossary.js`.
- **Скоуп тем:** сепия = `[data-gill-v16][data-gill-reader-theme="sepia"]` под
  `<64em`; ночь = общесайтовый `html.dark`.

## Открытые вопросы / что дальше

- ✅ **Сплошной проход завершён и проверен** — коммит `616b87c` (не смёржен).
  Реконсилено к базе v2.8/2.9 (astro check 0 ошибок, 0 JS-ошибок):
  - **part-TOC**: римские цифры разделов (счётчик level:2 → toRoman в
    `GillPartTocOverlay.astro`); двойные рамки → одна левая акцент-полоска
    (`.toc-part-item`, `border-left:2px; radius 0 12 12 0`); overlay `fixed`
    накрывает контент (замер 412×900); scroll-lock не срывается (`js/site-utils.js`).
  - **бары**: безрамочные/заподлицо/frosted (radius0, shadow none, blur22,
    top-hairline, fixed) через новые токены `--gill-mobile-bar-frost/-hairline`
    (без регресса `.toc-sheet`); top-bar frost-on-scroll (`.is-scrolled`).
  - **панели**: акцент-полоска карточек терминов, чип категории, объяснение теста
    на любой ответ.
  - Файлы: `css/floating-cluster.css`, `js/site-utils.js`, `GillPartTocOverlay.astro`,
    `GillLearningSheet.astro`, `GillSeriesMobileBar.astro`.
- ⏭️ **v2.9-рефайнменты (следующие, мелкие поверх):** TOC-футер «Поделиться» →
  «Печать/PDF»; RC-02 (текст выбранной кнопки в ночи); RC-04 (тач-зона скорости 44px).
- ⏭️ **Отложено проходом (крупная механика, не строить вслепую):** метр калибровки
  (CBM: шаг выбора уверенности → calScore → вердикт 3 градации, RC-03) + mastery-strip.
- ⏳ Бейдж «2» (счётчик выделений) наезжает на шестерёнку — `highlights-runtime.css`
  + JS-позиционирование.
- ⏳ Дедуп цитат — `js/highlights.js` (аудит).
- ⏳ Экран запуска теста — `references/gill-mobile/gill-quiz-launch-*`. ВАЖНО: НЕ
  показывать «сколько каких вопросов / сколько авторов» (ошибка референса).
- ⏳ Фото хедера + «лишние рамки».
- ⏸ **Hermenevtika** премиум-ридер (лист «Настройки») — PR #81, на паузе (сначала Гилл).
- ⏸ Редактура текста Гилла — запаркована (сначала мобилка, потом десктоп).

## Марафон-прогон (ночной, скриншот-проверки по 3 темам @412px)

Инструкция владельца: «делай прогоны и прогоны, приводи всё к идеальному, без
костылей и багов (двойные рамки, рамка вместо цифры и т.п.)». Метод: полная
сборка → скриншот каждого экрана в день/сепия/ночь → безжалостный разбор →
фикс → пересборка → повтор. Реальный тап (`locator.tap()`), не синтетический
`click()` в evaluate — иначе ложные фокус-кольца (`:focus-visible` эвристика).

### part-TOC (оглавление частей) — ✅ закрыт ранее
- Золотые римские во всех 3 темах; одна левая акцент-полоска (без коробок);
  Введение/Справ. без «рамки-пилюли»; футер = одна «Распечатать · PDF».
  Корень «бронзы вместо золота»: `--gb-accent-gold` переопределялся в сепия-
  скоупе на `#8a612f` (стр. ~4207) — перекрыт точечными правилами.

### Лист «Обучение» (термины/конспект/тест/заметки) — ✅ этот прогон
Найдено и починено 3 бага:
1. **Двойная «рамка» на вкладках** — при открытии листа авто-фокус садился на
   первую вкладку «Термины» и оставлял UA-кольцо; после перехода на «Тест»
   ВЫГЛЯДЕЛО как две выбранные вкладки. Фикс: `.gill-tab:focus{outline:none}` +
   `.gill-tab:focus-visible{outline:2px accent}` (кольцо только с клавиатуры).
   На реальном тапе кольца нет; со стрелок — только на сфокусированной вкладке.
2. **«Серия: 0» торчала заглушкой** в шапке теста. Заменено на логику эталона
   v2.9 `updateStreak()`: до ответа — пусто; серия≥2 → «🔥 N подряд»; иначе
   «Пройдено: N · рекорд M». Добавлены счётчики `quizSeen`/`quizBest`.
3. **Пустая панель «Заметки» с клавиатуры** — `buildNotes()` висел на `click`
   вкладки, а не в `activateTab()`, поэтому активация стрелками не строила
   панель (не было даже «Сохранённых заметок пока нет»). Перенёс постройку в
   `activateTab` (как у терминов), убрал отдельный `click`-листенер и мёртвый
   `notesBuilt`-гард; заметки перестраиваются при каждой активации.
- Термины: 28 карточек во всех темах, 0 JS-ошибок. Тест: один выбранный таб,
  чип категории (ЛЮДИ/ДАТЫ/МЕСТА/ТРУДЫ), варианты, объяснение на любой ответ.

### Бары + «Настройки» — ✅ этот прогон (замеры + скрины)
- Бары: `radius:0`, `shadow:none`, `backdrop:blur(22px) saturate(1.8)`,
  верх — без рамки, низ — hairline 1px, `position:fixed`; оверлеи накрывают
  контент (top:0 h:900 vh:900). Все 3 темы идентичны.
- Настройки: ЧТЕНИЕ/Настройки, ТЕМА(День/Сепия/Ночь) / РАЗМЕР (A·точки·A) /
  ИНТЕРВАЛ (Плотный/Обычный/Свободный) / ШИРИНА (Узкая/Обычная/Широкая).
  Сегмент темы синхронит `aria-pressed` через `applyGillReaderTheme` (тап в
  настройках, цикл в баре, restore на загрузке). Лишних фокус-колец нет.

### Тест (состояние ПОСЛЕ ответа) — ✅ проверено
Верно/неверно — зелёная/красная рамка+тон; блок «ПОЧЕМУ ЭТО ВАЖНО» с акцент-
полоской на любой ответ; «Перечитать: …» ссылка; «Следующий вопрос»; стрик
«Пройдено: 1». Обе темы, 0 ошибок. Без двойных рамок.

### Поиск по разделу — ✅ + баг починен
Реальные результаты («Кеттеринг» → «Найдено: 8»): карточки с заголовком раздела
и сниппетом, совпадение подсвечено золотом, троеточие-обрезка. НО: нативная
кнопка очистки `input[type=search]::-webkit-search-cancel-button` рисовала синий
× (мимо палитры). Фикс: `appearance:none` + mask-× цветом `--gb-text-muted`
(перекрашивается по теме). `css/floating-cluster.css`.

### Верхний бар + узкая ширина — ✅ проверено
- Верхний бар: scroll=0 → прозрачный (`is-scrolled` off, bg transparent);
  после скролла → frosted (`is-scrolled` on, bg translucent + blur22). Значок
  скорости «1×» на кнопке play. Обе темы.
- 360px (узко): вкладки обучения (Термины/Конспект/Тест/Заметки + иконка) в один
  ряд без переполнения; кнопки настроек не режутся («Свободный» целиком). 0 ошибок.

### Ещё отложено (не видимые баги, осознанно НЕ трогаю ночью)
- RC-04: тач-зона кнопок скорости (`.mobile-speed`, сейчас 32px). Эталон
  расширяет её псевдоэлементом `.mobile-spd::before{inset:-10px -2px}` БЕЗ роста
  пилюли. НО у нас `.mobile-speedrail` — `overflow-x:auto` (→ `overflow-y`
  тоже `auto`), а высота рейла = высоте пилюли (32px), поэтому вертикальный
  вылет `::before` обрезается скролл-контейнером и хит-тест не срабатывает.
  Чтобы техника эталона заработала, рейлу нужен `min-height:~44px` (пилюли
  центрируются, у `::before` появляется место) — но это может чуть подрастить
  верхний бар при speed-open (риск микро-прыжка). Делать под присмотром, не
  вслепую. Пустой `::before` без высоты рейла = мёртвый CSS, поэтому НЕ добавлял.
- Метр калибровки (CBM, RC-03) — крупная механика (шаг уверенности → 3 градации
  вердикта). Строить не вслепую. Бейдж «2» на шестерёнке. Дедуп цитат.

### Файлы прогона
- `css/floating-cluster.css` — `.gill-tab:focus/:focus-visible`; перекраска
  `::-webkit-search-cancel-button`.
- `GillLearningSheet.astro` — стрик по v2.9, `buildNotes` в `activateTab`.
- Скрипты проверки (git-excluded): `scratch_mara_learn.js`,
  `scratch_mara_bars_settings.js`, `scratch_mara_parttoc.js`,
  `scratch_mara_quiz_answer.js`, `scratch_mara_search_topbar.js`,
  `scratch_mara_narrow.js`.

### Итог прогона
Ядро ридера (part-TOC · бары · настройки · лист обучения со всеми вкладками ·
тест до/после · поиск) проверено по скринам в день/сепия/ночь на 412 и 360px,
реальными тапами. Починено 4 бага: двойная рамка вкладок, «Серия: 0», пустые
заметки с клавиатуры, синий × поиска. 0 JS-ошибок. Коммиты (не смёржены):
`0a6329d`, `2b255dc` поверх `bc593c7`/`7a5a0ab`.

## Правила окружения (чтобы не тупить)

- Полная сборка: `npm run strangler:build:production-like` (~4 мин, включает
  `copy-legacy-to-dist` + cache-bust). Плейнрайт: chromium на
  `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, `chromiumSandbox:false`.
- SYSTEM-правки (css/js) — на ветке `lane/system-*`, коммит-префикс `[LANE ...]`.
