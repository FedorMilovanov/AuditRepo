# Глубокая source-верификация gb-is-my-strength — Часть 5 (SEO, метаданные, CSP, IndexNow, runtime-кэш)

**Дата:** 2026-08-05 · **Проверено на:** `main@4ce39dc816727c43373491acfb5bad0916cde113`
**Серия:** Части 1–4 + эта Часть 5.

---

## 1. ✅ Подтверждены (7)

| ID | Evidence | Вердикт |
|---|---|---|
| **D-19** | **Обе половины живы**: `AntisovetovPageHead.astro:16` `<title>…| Господь Бог</title>` vs `:26` `og:title` без суффикса; `Rimlyanam7PageHead.astro:14` `<title>…| Господь Бог — Сила Моя</title>` vs `:22` `og:title` без | 🔴 подтверждён (обе половины) |
| **BUG-SEO-001** | `deploy.yml:327-343`: IndexNow-сабмит с `continue-on-error:true` + `curl --fail-with-body` **без ассерта 200/202**, ретраи 3, два эндпоинта | 🔴 подтверждён |
| **AR-IDX-05 (усилен)** | `SITE_CONFIG.version` (хардкод `1778943682`) — cache-buster в runtime-инъекциях: `enhancements.js` → `/css/enhancements-runtime.css?v=`+version; `highlights.js` → `/css/highlights-runtime.css?v=`+version | 🔴 **усилен**: runtime-CSS намертво закэшированы; инвалидация невозможна, пока version не двигается |
| **Speakable** | **109** файлов с `speakable` (Hermenevtika, Gill-части, Antisovetov, Diotrophes, ChtoBibliya…) — ru неэлигибелен, но разметка осталась (W8) | 🔴 подтверждён (открытый W8-пункт) |
| **NEW-OG-SIZE-PARAM** | allowlist-подход подтверждён (global shared, не per-route) — суть остаётся | 🟡 как в матрице (PARTIAL) |
| **RSS** | `rss-feed-normalizer.js:94-98` — сортировка по дате desc + route tiebreak; `lastBuildDate` из manifest `generatedAt`; это уже нормализованный реестр (W2-часть закрыта) | 🟡 W2-RSS сделан; остаётся видимость дат `<time>` |

## 2. 🟢 Реальность ЛУЧШЕ — кандидаты на сужение/закрытие (2)

| ID | Что изменилось | Evidence | Вывод |
|---|---|---|---|
| **AR-IDX-10** | «CSP legacy и Astro расходятся (jsdelivr добавлен только в Astro)» | Корневой `index.html:10` CSP теперь содержит `cdn.jsdelivr.net` и `*.aws.cdn.hf.co` — **совпадает** с `HomePageHead.astro` и `HermenevtikaPageHead.astro:8` | 🟢 **не воспроизводится** — CSP унифицирован (как минимум для home/hermenevtika); проверить все 15 legacy html и закрыть/сузить |
| **IndexNow-пути** | `indexnow.yml` — это **Diagnostics/Readiness**, а не сабмит; сабмит в `deploy.yml:327-343`; пути `push: paths: ['**']` | Фактический сабмит один (deploy), D-1 (две группы) остаётся, но «два эндпоинта» в одном шаге — намеренно (api.indexnow + yandex) | 🟡 уточнить формулировку BUG-SEO-001: «нет ассерта 200/202» — верно; «два эндпоинта» — by-design |

## 3. 🆕 Новые наблюдения

1. **AR-IDX-05 — переоценка severity.** Хардкод `SITE_CONFIG.version` = `1778943682` заморожен с 14.07. Он управляет версиями runtime-CSS (`enhancements-runtime.css`, `highlights-runtime.css`), которые инжектятся скриптами. Это означает: **любое будущее изменение этих CSS не дойдёт до пользователей** (старый `?v=` уже в их кэше). Строка должна быть **P1/P2, а не P3**, и чиниться в рамках W2 (детерминированный build-id) или хотя бы поднятием числа.
2. **Speakable-разметка — 109 файлов** — это не «несколько страниц», а широкий слой; W8 «убрать Speakable (ru неэлигибелен)» — реальный объём.
3. **D-19 — обе половины открыты**, при том что session log 07-11 говорил «rimlyanam-7 половина закрыта». Факт: у `Rimlyanam7PageHead.astro` `<title>` всё ещё с суффиксом «| Господь Бог — Сила Моя», а `og:title` — без. Значит, «закрытие» 07-11 касалось другого места (или title-канон поменяли обратно). Строку надо пересмотреть.

## 4. Итог всей серии (Части 1–5)

| Метрика | Значение |
|---|---|
| Открытых строк проверено кодом | **~95 из 145** |
| Подтверждено на `4ce39dc8` | **~67** |
| Реальность лучше (сужены/кандидаты на закрытие) | **~19** (добавились AR-IDX-10, часть RSS/IndexNow) |
| Реальность хуже (усилены) | **~8** (AR-IDX-05 → P1/P2, D-19 обе половины, 49 воркфлоу, бюджеты, anchors/glyphs 0/0, 366/31 слушатели, speakable 109) |
| Browser-класс | ~35 строк |

**Обновлённый топ действий:**
1. **AR-IDX-05 — поднять severity и починить** (кэш-баст runtime-CSS мёртв; пользователи застрянут на старых стилях при будущих правках).
2. **D-19 — пересмотреть**: обе половины открыты, заявление 07-11 о rimlyanam-7 не подтверждается кодом.
3. **AR-IDX-10 — reverify и закрыть** (CSP унифицирован).
4. Закрыть STRANGLER-HYGIENE / ENGINE-P1-27 / MAP-P1-06.
5. Бюджеты обновить; Speakable-массу (109) внести в W8-план.

*Документ — untracked в AuditRepo; коммиты/пуши не выполнялись.*
