# GB Baptist content-truth closure + main advance — 2026-08-08

## Result

Product `#1217` has merged and closes the active `BAPT-CONTENT-TRUTH-01` root.

New Product `main` anchor:

`670d82fa5b567813a52886c62c704047f76b4b71`

Previous anchor:

`a068decefff4ddd0055da952c84b7a3633d7b43b`

Exact main advance is one commit / one file:

`baptisty-rossii/research/55-qa-open-questions-and-discrepancies-register-2026-06-19.md`

No Home, Search runtime, S12 body/metadata, CSS or shared runtime file changed in this main advance.

---

## Closure evidence — BAPT-CONTENT-TRUTH-01

PR:

`#1217` — `docs(baptisty): resolve current publication-truth discrepancies`

Exact PR head:

`e1bbc4746904a5c7639c49146c4d434f74bdb038`

Merged Product commit:

`670d82fa5b567813a52886c62c704047f76b4b71`

PR state:

- closed;
- merged = true;
- draft = false at merge;
- changed files = 1;
- review threads = 0.

Exact-head workflows on `e1bbc474...` are all terminal SUCCESS:

- Shared Files Guard;
- Metadata & IndexNow Readiness;
- Editorial Dateline Contract;
- Glossary Contract;
- Visual Parity Guard.

### Q4 — 1923 arrests

The QA discrepancy register now closes Q4 on the stronger current documentary authority:

- **11** arrested delegates;
- A. I. Savin publication of Anti-Religious Commission/OGPU material;
- footnote to protocol no. 41, RGASPI f. 17, op. 112, d. 565a, l. 46;
- secondary `21` is retained only as historiographic divergence, not equal current authority.

The same register summary now states 11 arrested at the XXV Baptist congress in 1923.

### Q8 — Petersburg meeting date

The register now closes Q8 as:

`1–6 April 1884`

with current support from:

- Filipp Nikitin (2020), p. 136;
- his cited Stepanov 2016, pp. 106–107;
- independent 1 April opening witness in `Bratskiy Vestnik`.

The register summary now explicitly distinguishes the Petersburg meeting on 1–6 April from Novo-Vasilyevka on 30 April–1 May.

### C8 narrowing

The archive target for 1923/1926 protocols remains, but is correctly narrowed: Q4 no longer depends on obtaining the full archive package because the current corpus already has documentary support for the public `11` claim.

This means the earlier mismatch — public prose stronger/more precise than the QA authority register — is resolved without rewriting already-correct public bodies.

`BAPT-CONTENT-TRUTH-01` therefore leaves active MASTER in this closure wave.

---

## Matrix effect

- active work units: **13 → 12**;
- direct current defects: **3 → 2**;
- verified necessary improvements: **3**;
- system verification lanes: **4**;
- owner decisions: **3**.

No legacy mega-copy is needed; Product PR/history plus this compact closure report preserve provenance.

---

## Current owner refresh after main moved

The main move immediately changed ancestry/ownership facts for other active lanes.

### BAPT-S12-01 — #1218

`#1218` has refreshed onto current `main@670d82fa...`.

Actual current head:

`a70b42d352eaa422092a3e5564a93ae929bfff35`

Current compare:

- ahead = 2;
- behind = 0;
- exact semantic files remain only:
  - `scripts/sources-hygiene.js`;
  - `src/components/baptisty-rossii/BaptistyRossiiPodpolnayaPechatBody.astro`.

Fresh exact-head CI is not terminal yet. An older Shared Files run on this moving candidate is cancelled and a newer Shared Files run is pending; other returned groups are queued. Do not confuse the predecessor's green record with current-head merge authorization.

The Spravochnik PageHead metadata residual and PageHead scan blind spot remain unchanged because main advance `670d82fa...` touched only the QA register.

### CATALOG-PROJECTION-01 — #1216 → #1221

`#1216` is now closed unmerged and explicitly `SUPERSEDED_VERIFIED`.

Canonical successor:

`#1221` — current actual head `b603b0c9a51407d721db99d7ed1ae0364a6f7585`

Current compare against `670d82fa...`:

- ahead = 2;
- behind = 0;
- 5 intended files:
  - `data/scripture-search-index.json`;
  - `scripts/articles-visual-parity-audit.js`;
  - `src/components/articles/ArticlesLibrarySection.astro`;
  - `src/components/articles/ArticlesMain.astro`;
  - removal of `src/components/articles/ArticlesPublicationsSection.astro`.

The predecessor-only Scripture autofix workflow mutation was intentionally not transferred. This is a materially cleaner bounded owner.

All returned exact-head workflows for `b603b0c9...` were still queued/pending at recheck; no merge authorization yet.

### SYS-CURRENT-GOLD-READINESS — #1220

`#1220` has also refreshed onto current main.

Current actual head:

`98b1e55c0fd5b3c7db1005552d9d128dab8e294c`

Compare against current main is behind=0 and still only changes:

- `.github/workflows/deploy-candidate-contract.yml`;
- `scripts/human-reachability-audit.js`.

The source-level witness gap recorded in the preceding verification remains applicable: the code bytes of `human-reachability-audit.js` are unchanged from the audited `9cdc798...` implementation; subsequent commits only brought in the new QA-register main ancestry. Hidden/aria-hidden ancestor and CSS-hidden links can therefore still satisfy the current static regex witness model.

Fresh exact-head CI is queued/pending.

### SEARCH-P3-02 — #1209

The Product main move makes current `#1209` head `2b90612c...` stale by one commit:

- merge base remains `a068dece...`;
- behind = 1 against `670d82fa...`;
- GitHub currently reports mergeable=false.

Earlier green/in-flight checks are historical evidence only until Search refreshes current-main ancestry and obtains exact-head terminal proof.

### #1212 runtime census

`#1212` is now behind current main by 2 commits. It remains audit-only guard work and is not a current Product/Home repair owner.

---

## Home / temporary-file continuity

Because the exact Product main advance `a068dece... → 670d82fa...` changes only the Baptist QA register:

- Home source remains byte-unchanged from the prior Home recheck;
- no new Home defect is introduced by this merge;
- the optional Directions/Ambient owner-convergence disposition remains unchanged;
- absence of `astro.config.dev.mjs` on the prior exact source tree remains applicable to the new main advance (the merge does not add any file outside the one QA register path).
