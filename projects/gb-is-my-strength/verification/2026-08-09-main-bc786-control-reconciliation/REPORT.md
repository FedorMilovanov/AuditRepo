# GB control reconciliation — Product main `bc786f4d…`

**AuditRepo branch base:** `5154a5ef11c240e92e5758bbc7ac6445db9ae7f7`  
**Product authority observed:** `bc786f4da7b6b3e9924caa046a3ab9ba829330fe` (`#1373` merged)  
**Mode:** AuditRepo SSOT reconciliation only; Product is read/verify.  
**Stop criterion:** current merged truth, active/protected owners and exact next proofs are classified without stale/duplicate active rows. No Product lane takeover and no physical legacy move/delete.

## Executive control result

The previous AuditRepo snapshot was materially stale, not merely one SHA behind:

- Product `#1386` is merged; truthful merged Strangler readiness is **3 dependency blockers**.
- Product `#1373` is merged as `bc786f4d…`; native article quiz score-tier, badge and short/full explanation parity is no longer an active systemic repair root.
- Lot publication PR `#1339` is **closed unmerged / superseded**. It is historical publication evidence, not an active owner and not merge authority.
- Lot Bible corpus PR `#1389` is **closed unmerged / rights-blocked** under the binding Research rights/provenance decision. It must not be treated as an approved Lot dependency.
- Product issue `#1295` remains open and current Product `main` has no `src/pages/articles/lot-i-sodom/index.astro`; Lot is therefore still an unpublished authoring layer, not a production route.

This wave removes those stale owner claims and records the actual continuation boundary.

## Strangler retirement — exact current boundary

### Merged truth

Merged `main@bc786f4d…` still has **3 dependency blockers**:

1. `scripts/baptisty-roadmap-audit.js`
2. `scripts/readable-audit.js`
3. `scripts/owner-ui-regression-guard.js`

Physical move/delete remains unauthorized.

### Active Baptist owner — Product #1395

Draft `#1395`, head `6a2c14f2bc47a54b30e4ab7a6893704274bfffe0`, is current-main based and changes exactly three files. Its exact-head Shared run already proves:

- legacy reference inventory: **32 dependencies / 2 dependency blockers**;
- Baptist roadmap publication-authority audit: PASS;
- retirement readiness: `NOT_YET_SAFE_TO_MOVE_OR_DELETE; blockers=2`;
- Shared/Metadata/Node/Deploy: SUCCESS at observation time;
- no review threads were open.

Visual Parity was still queued at the observation point. Therefore `2` is proven candidate-head evidence, **not merged authority yet**. The #1395 owner must finish terminal exact-head gates, final `behind=0`/race/review check and merge itself.

### Protected readable owner

Branch `agent/readable-audit-reference-authority-20260809` has real unique work but is ancestry-stale against `bc786f4d…` (`ahead=2 / behind=2`). It replaces route-specific root HTML reads with `buildAuditProSourceCorpus()` / public-surface authority while preserving readable/publication assertions and adds direct execution coverage.

Do not duplicate this work. Its owner must absorb fresh main without force/rebase, reconcile the ledger only after #1395 settles, prove the next truthful blocker transition, and re-earn exact-head gates.

### Protected owner-UI owner

Branch `agent/owner-ui-reference-authority-20260809` also has real unique work but is ancestry-stale (`ahead=1 / behind=2`). It keeps owner-approved UI markers/invariants and changes retained route reads to `buildAuditProSourceCorpus()` rather than weakening the assertions.

Do not duplicate this work. Its owner must refresh after the preceding retirement slice, update the ledger in its own bounded transaction and prove the final dependency-blocker transition to zero.

### Hidden move-safety owner is **not implemented yet**

Branch `agent/legacy-inventory-storage-authority-20260809` currently has **no unique delta** (`ahead=0`) and is already behind current main. Its name is reservation history, not an implementation.

The hidden risk from the earlier audit therefore remains: `scripts/legacy-reference-inventory-audit.mjs` is excluded from its own dependency census, while immutable reference verification still resolves bytes through physical `ROOT/<legacyPath>` storage. A future physical quarantine can therefore break the inventory owner even after dependency readiness reaches zero.

Required final technical slice after dependency blockers reach zero:

- make inventory reference-byte resolution storage-aware through the canonical legacy-reference path/resolver authority;
- preserve immutable SHA/word/H1/H2 checks and ambiguity fail-closed behavior;
- do not weaken or exclude references merely to obtain green;
- prove normal Shared inventory + retirement readiness on exact head;
- perform a non-destructive quarantine dry-run proving all references resolve after root-path removal simulation.

Only after that evidence can a **separate, explicitly authorized physical move lane** be considered. Move/delete is not part of this reconciliation wave.

## Lot — current publication boundary

### What is closed upstream

- Authoring layer remains accepted in Product main.
- Catalog/human-reachability owner `#1348` is merged; do not recreate a one-off Lot card.
- Search role authority `#1313` and Scripture occurrence writer `#1353` are merged.
- Shared native quiz parity `#1373` is merged; retire the systemic quiz repair root.
- `#1339` is closed unmerged/superseded and cannot be used as current release authority.
- `#1389` is closed unmerged/rights-blocked and cannot be used to expand Scripture corpus without new binding rights/provenance evidence.

### What is actually open

- Product issue `#1295` remains the Lot root.
- Product `main@bc786f4d…` has no Lot route file, so publication still requires a **fresh `release/*` successor from current main** after adjacent owners settle.
- `#1378` owns the bounded Lot source-link resilience repair.
- `#1401` owns shared standalone-footer runtime extraction; Lot must consume the resulting shared owner later rather than copy it.
- `#1334` remains the separate Avraam/Tall el-Hammam owner.

### Lot content/publication residuals to carry into the fresh successor

These remain acceptance requirements unless a fresh owner proves them fixed or invalid:

- canonical `#website` JSON-LD / `Article.isPartOf` publication graph;
- positive canonical Bible-tooltip projection/witness without using rights-blocked corpus expansion;
- TOC inclusion for `#sec-map-connection`;
- Lot journey semantic omission: Egypt → Negev → Bethel/Ai before Genesis 13 separation;
- mobile-readable semantic SVG presentation, not merely overflow-free shrinking;
- print/PDF visibility for hidden-base `.reveal` figures;
- quiz **content** quality (teaching explanation depth and plausible distractors), distinct from the now-closed shared runtime bug;
- source-link resilience and narrowed Numayra annotation;
- fresh Search/RSS/sitemap/Scripture projections only through canonical writers;
- exact-head browser/SEO/source/print/accessibility evidence and post-merge public witness.

### Media truth

`lane/lot-media-20260809` is currently `ahead=0`: there are no unique media bytes to ship. `lane/lot-illustration-placement-20260809` has a real seven-file source placement delta but is heavily diverged from current main.

Therefore the old “14 illustrations ready” wording is not release evidence. A future media/placement owner must explicitly choose and prove an acceptance boundary (for example the nine source-grounded placements vs a larger intended family set), provide actual responsive assets + Lot-specific OG, then assert the exact positive rendered count across browser and print/PDF witnesses.

## Other active agents — no-takeover instructions

- **#1395 Baptist Strangler:** finish exact-head barrier and merge only if `blockers=2` remains true.
- **Readable Strangler branch:** refresh after #1395, keep storage-aware corpus semantics, update ledger in-lane, prove next blocker transition.
- **Owner-UI Strangler branch:** refresh after readable, preserve every owner UI assertion, update ledger in-lane, prove dependency blockers reach zero.
- **Inventory storage authority:** no implementation currently exists; create a fresh bounded owner only after earlier blockers settle, then prove quarantine dry-run safety.
- **#1393 Home Search settled-state:** preserve its dedicated owner; fix the transient invalidation witness rather than opening another lane.
- **#1363 MapScale:** semantically proven historically; perform one final current-main ancestry refresh and exact 3/3 only after the Strangler mini-wave settles.
- **#1378 Lot sources:** finish only source resilience scope.
- **#1401 standalone footer:** finish shared footer ownership without Lot-specific content.
- **#1334 Avraam:** keep Atlas/retraction repair separate from Lot.
- **#1402 Baptist media coverage:** audit/measurement lane only; do not promote to Product repair without a confirmed current defect.

## Logical session end

This reconciliation defines the nearest honest logical end for the current session:

1. AuditRepo no longer represents merged #1373 or closed #1339/#1389 as active implementation owners.
2. Strangler merged truth stays 3; candidate #1395 proof is separately recorded as 2 without prematurely promoting it to merged truth.
3. Two real protected reader branches are preserved for their owners; the supposedly reserved inventory branch is correctly classified as empty/unimplemented.
4. Lot is explicitly classified as **unpublished on current main**, with fresh-successor requirements separated from rights-blocked and stale publication history.
5. No Product branch is modified or taken over.
6. No physical legacy move/delete is authorized.

The next session should start from live Product main and these owner transitions, not from the older #1339 / readiness-11/5 snapshots.