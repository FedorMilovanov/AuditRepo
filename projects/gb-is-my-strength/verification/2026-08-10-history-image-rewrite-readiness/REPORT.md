# Historical Image Rewrite — Final Execution Receipt

Date: 2026-08-10  
Product: `FedorMilovanov/gb-is-my-strength`  
Audit authority: `FedorMilovanov/AuditRepo`

## Final status

**HISTORY_REWRITE_COMPLETED_AND_VERIFIED**

The historical-image owner-managed Git history rewrite was executed after freeze, durable preservation, final branch cemetery, fresh inventory and a fresh local dry-run. The rewrite completed successfully and the Product working tree at `main` was proven byte-identical before and after the history rewrite.

## Frozen execution anchor

Pre-rewrite `main`:

```text
48cdbfc6905ef70239319a579d26869addc8bc36
```

Post-rewrite `main`:

```text
b8c92eda3af96158dbee4ba53803e90c30cce31c
```

Current GitHub commit lookup resolves the rewritten SHA to the same semantic Product commit:

```text
fix(reader): close shared control semantics residuals (#1463)
```

Main tree before and after:

```text
9d194c47acfe04874440851990a8228e88c45fed
```

Therefore current Product source/runtime/content/generated bytes represented by `main` are unchanged by the history operation.

## Final branch cemetery

Before final inventory, exactly 23 refs that already had AuditRepo SAFE DELETE / SUPERSEDED — SAFE DELETE receipts were removed atomically. No branch was deleted by namespace pattern or age alone.

Execution receipt:

```text
cemetery refs removed = 23
already absent on first successful cemetery transaction = 0
```

A subsequent fail-closed retry observed all 23 as already absent and did not recreate or re-delete them.

After cemetery and before diagnostic retirement, final managed graph was:

```text
heads = 100
tags = 99
commits = 5237
main tree = 9d194c47acfe04874440851990a8228e88c45fed
```

The diagnostic branch was retired after successful post-rewrite verification, so normal remote branch count is one lower than the pre-retirement `heads=100` snapshot.

## Final purge set and durable preservation invariant

Final freshly computed purge set remained:

```text
historical-only image blobs = 510
bytes = 522,386,373
MiB = 498.2
```

The final transaction machine-proved before any live rewrite:

```text
PURGE_SET ⊆ DURABLY_PRESERVED_SET
```

No newly discovered purge blob was outside the durable 510-OID preservation manifest.

Original preservation archive reconstruction proof remains:

```text
bytes = 522,804,227
SHA-256 = 6bdc127c03d73619f8dfabebb10f5e0b5d18419052d4933137a10c6f3fac6027
```

Base durable Filen location:

```text
/ARCHIVE/gospod-bog/repo-history/2026-08-image-purge/
```

## Fresh final recovery and dry-run evidence

A new recovery bundle was created from the exact final frozen pre-rewrite owner-managed graph and verified before the live push.

Fresh final recovery bundle SHA-256:

```text
5164d6609dbedb26fdd49c156c29cb506d1dc61b1a1d1a1488fb4fda1d926a20
```

Fresh final dry-run evidence SHA-256:

```text
3c9404b7d06c6345f4de87f6dd3f46b5489b3f42e456feb0ba444e93c8c95f9b
```

Fresh final durable Filen location:

```text
/ARCHIVE/gospod-bog/repo-history/2026-08-image-purge/final-freeze-48cdbfc6905e-20260810-031908/
```

These were uploaded and positively listed in Filen before the destructive push.

## Final dry-run result

Pinned `git-filter-repo 2.47.0` local final dry-run completed successfully:

```text
main tree identical = true
purge blobs = 510
commit count = 5237 -> 5231
mirror size = 581.0 MiB -> 135.4 MiB
post-rewrite git fsck = clean
managed head/tag ref names preserved = true
purge blobs unreachable from rewritten owner-managed graph = true
```

The frozen remote graph was then re-read and proven exactly equal to the graph used by the dry-run immediately before the live force-update.

## Live rewrite transaction

The live transaction used one atomic forced update over the remaining owner-managed heads and tags:

```text
remaining owner-managed heads + tags at push = 199
atomic push = success
```

No branch-protection weakening or blind `git push --mirror` was used.

GitHub internal `refs/pull/*` were intentionally not included and remain outside owner control.

## Post-rewrite verification

After the live push, a fresh remote fetch was performed and verified:

```text
objects checked = 85,566
commits in commit graph = 5,231
git fsck --full = clean
remote main = b8c92eda3af96158dbee4ba53803e90c30cce31c
remote main tree = 9d194c47acfe04874440851990a8228e88c45fed
```

The post-rewrite tree equals the pre-rewrite tree exactly.

## Diagnostic PR #1460 retirement

Diagnostic branch:

```text
audit/history-image-bloat-20260810
```

was deleted only after successful post-rewrite verification.

GitHub now reports PR #1460:

```text
state = closed
merged = false
draft = true
```

Therefore none of its five temporary diagnostic workflows entered Product `main`.

## GitHub server-side size caveat

The owner-managed mirror reduction is proven locally, but GitHub repository metadata may remain large because historical blobs are still reachable from GitHub-managed read-only `refs/pull/*` and server garbage collection is outside owner control. Do not interpret API repository-size metadata as evidence that the owner-managed rewrite failed.

No attempt was made to mutate or delete GitHub internal PR refs.

## Recovery boundary

If rollback is ever required, use the fresh final recovery bundle from:

```text
/ARCHIVE/gospod-bog/repo-history/2026-08-image-purge/final-freeze-48cdbfc6905e-20260810-031908/
```

and verify SHA-256:

```text
5164d6609dbedb26fdd49c156c29cb506d1dc61b1a1d1a1488fb4fda1d926a20
```

before restoring any refs under a coordinated write freeze.

## Resumption rule

All clones/worktrees created before the rewrite contain obsolete commit SHAs. Agents must not push from those old histories.

The canonical Product continuation anchor is now:

```text
main@b8c92eda3af96158dbee4ba53803e90c30cce31c
```

Agents should fresh-clone or otherwise explicitly reset/rebase their workspace against the rewritten remote authority before resuming Product writes.

## Closure

The historical image bloat cleanup is complete for owner-managed Git history:

- durable preservation: **PASS**
- branch cemetery required by this cleanup: **PASS**
- final inventory: **PASS**
- final dry-run: **PASS**
- `PURGE_SET ⊆ DURABLY_PRESERVED_SET`: **PASS**
- atomic live rewrite: **PASS**
- main tree identity: **PASS**
- fresh post-rewrite `git fsck`: **PASS**
- diagnostic PR closed unmerged: **PASS**
- diagnostic branch removed: **PASS**

No further history-image rewrite action is required before normal coordinated Product work resumes.