# Terminal report — V10-READER-SPEEDRAIL-SSR-FOCUS

Date: 2026-08-10
Disposition: **TERMINAL / MERGED-GREEN**

## Product result

- Product PR: `FedorMilovanov/gb-is-my-strength#1553`
- Exact tested head: `8874bd7d9f548b0b4720986cc428418d2a9dba68`
- Merge commit / current-main integration commit: `36ab77b443a1c709ca54ba6b845dfe28cabceeae`
- The shared ReaderChrome SSR boundary now removes `.mobile-speedrail[aria-hidden="true"]` from rendered geometry before hydration while preserving the existing hydrated roving-radio behavior once runtime activates the rail.

## Permanent proof

The exact PR head completed the authoritative `Reader Controls Accessibility` Chromium + WebKit contract. Its permanent no-JS 390×844 witness covers representative Articles, Hard Texts and Baptists readers, verifies zero geometry for the six hidden speed controls, excludes the hidden rail from sequential Tab navigation and performs a full built-output family census. Shared Files Guard, Source Authority, Search Modal, Deploy Candidate, Scripture, Native Source, Print, Glossary, Overlay Runtime and visual pixel-diff checks were also green before merge; the Runtime Interactive Home/WebKit half was green and the repository merged the PR after its protected checks reached acceptance.

## Terminal conclusion

`V10-READER-SPEEDRAIL-SSR-FOCUS` is resolved on merged `main` and must not remain active in MASTER. Reopen only on fresh current-main evidence.
