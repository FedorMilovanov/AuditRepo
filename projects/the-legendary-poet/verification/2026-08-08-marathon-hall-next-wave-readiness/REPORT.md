# Marathon Hall Next Wave Readiness — TLP @ `c34debc7` — 2026-08-08

**Product anchor:** `c34debc7ec3cf769261779d763f21f617a3500a2` (metricGreybox active, 3 waves merged: foundation `9cce8bb` + reference `cc81858` + tooling `c34debc7`)  
**AuditRepo base:** `a66c785` → `92282f1` marathon (9 commits)  
**Scope:** readiness for bounded H1/H2/H3 neutral metric candidate authoring (WORK_QUEUE.md next transaction)

---

## Current source witness (exact)

- `docs/hall-v3/hall-v3-contract.json` `phase=metricGreybox`, `foundation=completed`, `referenceBible=completed`, `metricGreybox=active`, all later gates `blocked`
- `greybox-tooling.json` Blender `4.5.12 LTS` build `84afd5f785f7` proven (download + sha256 + headless save/reopen, smoke `.blend` 390K artifact `9018818728`)
- `greybox-candidates.json` H1/H2/H3 `unbuilt`, `approvedCandidate=null`, `approvedRig=null`, `phase=metricGreybox`
- No Hall `.blend`, GLB, geometry, material/lighting, camera rig, or runtime asset in `main` — `/hall` remains 8K placeholder
- Hall-v2 `src/components/hall/*` excluded from TypeScript contract, marked forensic, Three/R3F blocked during foundation

## Next bounded transaction (WORK_QUEUE.md)

Author **all three** neutral candidates from one shared deterministic Blender source under equal-quality constraints:

1. Start from fresh `main@c34debc7`, re-check open PRs
2. Keep `metricGreybox` active, later gates blocked — this wave creates evidence, not winner
3. Use proven Blender 4.5.12 + metre contract (1.0 scale `METERS`, collections `COLL_CAMERAS`/`COLL_CORE`/`COLL_EXPORT_HELPERS`, `HUMAN_PROXY` 1.75m)
4. Common provisional lens/FOV for all (instrumentation, not Camera Approval)
5. H1 = orientation court + chronological branches, H2 = promenade with compression/release + focus bays, H3 = asymmetric diagonal/curved + side rooms (not 3 cosmetics of one nave)
6. Same Pushkin proxy grammar (anchor portrait + secondary field + document pocket + return)
7. Evidence per candidate: dimensioned plan, 2 sections, 6 desktop cameras, 3× 9:16 mobile crops, top-down sightline, route length, forced-turn count, visible-next-destination
8. Neutral grey only (no bloom/fog/particles/gold), workbench render acceptable as evidence camera
9. `.blend`/PNG/SVG/JSON in Actions artifacts, deterministic scripts/manifests only in commit
10. Machine-validate accessible minima + equal completeness
11. Adversarially inspect actual artifact images/metrics after generation, not script names
12. Merge only if 3 packages comparable; selection/rejection separate transaction

**Automatic rejection:** one candidate more polished, ornament rescue, FPS/free-look needed, repeated equal niches, unfinished backs, Pushkin as UI card, accessibility violation, mobile rebuild needed, central space no job, sunk-cost retention.

## Evidence completeness

- 3 verification waves PASS: foundation (Hall v3 contract, 8K budget, legacy excluded), reference (Reference Bible), tooling (Blender 4.5.12 smoke 390K)
- `WORK_QUEUE.md` Hall spec is machine-checkable (equal-quality constraints, rejection criteria)
- No new MASTER row — correct per DOC_MAP (architecture lane outside matrix)
- Next wave must produce 3 comparable offline evidence packages (not production WebGL)

## TLP hygiene

- `verified/MASTER_BUG_MATRIX.md` 0 active (intentional, 15 historical retired to `archive/superseded`)
- `SYSTEM_THEMES` 9 themes all absorbed/closed (W0-W7, media provenance 5 active/24 exclusions)
- `incoming` 4 agents (56K, 2026-08-05), `verification` 20 waves (268K), `working` 5 files — clean
- No new engineering bug — fresh current-head verification required for any new finding

